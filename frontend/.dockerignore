# Build context for the FRONTEND image (context = ./frontend).
# node_modules is reinstalled in-image; .next is rebuilt.
node_modules
.next
out
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env / secrets (NEXT_PUBLIC_API_URL is passed as a build arg instead)
.env.local
.env.development.local
.env.test.local
.env.production.local

# VCS / IDE / OS
.git
.gitignore
.idea
.vscode
.DS_Store
Thumbs.db

# The image files themselves
Dockerfile
.dockerignore
