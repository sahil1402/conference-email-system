"""Auto-reply (Lane 1) routes.

Stub only — no routes implemented yet. Endpoints arrive in Phase 1.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/auto-replies", tags=["auto-replies"])
