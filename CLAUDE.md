# CLAUDE.md — Conference Email System
# Project memory. Read fully at the start of every session before writing any code.

## Codebase Memory (codebase-memory-mcp)
When this MCP server is available, prefer graph tools over grep/Explore
for structural code questions. Graph queries return precise results in
a single tool call (~500 tokens) vs file-by-file exploration (~80K tokens).

## Project Overview
AI-powered conference email management for AAAI/NeurIPS/ICML/ICLR. Two-lane workflow:
- FAQ Lane: high-confidence emails auto-replied using retrieved policy text only
- Human Review Lane: ambiguous/sensitive emails get an AI draft + chair approval

## Collaborators
- Sahil — lead developer · Prof. Yan — PI / stakeholder
- Jiacheng — collaborator (constraint: no specific model names in any design documents)

## Project Path
`D:\USC\The Melady Labs\conference-email-system`

## State Note
main includes Jiacheng's Phase 7 work (HEAD c4ed3f5) and the SQLite→PostgreSQL work (Docker Postgres db service, single-source DATABASE_URL, SYNC_DATABASE_URL removed, dialect-agnostic JSON accessors). Two deliberate nuances: config.py's DATABASE_URL default is SQLite (safe local/test/CI fallback; Compose injects Postgres for the container), and external_api is excluded from the MODEL_PROVIDER Literal (local covers it). Detail in the PG-migration entry + Current-Status "PG migration" row.

## Tech Stack
| Layer | Technology |
|---|---|
| Frontend | Next.js 14 + TypeScript + Tailwind CSS v3 + shadcn/ui |
| Backend | Python + FastAPI + async SQLAlchemy |
| Database | PostgreSQL via Alembic (migrated from SQLite, now on main); SQLite remains the safe default fallback for local dev/test/CI per config.py |
| AI | Cloud API, self-hosted OpenAI-compatible endpoint, template, or fallback — swappable via config |
| Retrieval | BM25 (rank_bm25), FAISS dense vectors, or RRF fusion — swappable via config |
| Testing | pytest + pytest-asyncio (`ml` marker for embedding-heavy tests) |

Dependencies via `backend/pyproject.toml` (no `requirements.txt`). Windows `.venv` uses plain `pip` (not `--break-system-packages`, which is Linux PEP-668 only).

## Architecture Rules (Non-Negotiable)
Six modules stay separate and independently replaceable:
1. Classifier (intent + confidence) · 2. Retriever (policy lookup) · 3. Router (faq vs human_review) · 4. Drafter (AI reply) · 5. Persistence (repositories only, never raw SQL in pipeline) · 6. UI (Next.js, never mixed with backend logic)

## Config Flags (`backend/app/core/config.py`)
Typed pydantic-settings `Settings`; env → `.env`. Access: `from app.core.config import settings` (cached via `get_settings()`). The first flags are the swappable module seams.
| Flag | Purpose | Default |
|---|---|---|
| MODEL_PROVIDER | Drafter provider (`anthropic_api`\|`anthropic`\|`local`\|`template`\|`fallback`) | `anthropic_api` |
| CLASSIFIER_BACKEND | Classifier (`keyword`\|`trainable`) | `keyword` |
| RETRIEVAL_BACKEND | Retriever (`bm25`\|`faiss`\|`fusion`) | `bm25` |
| ROUTING_STRATEGY | Router lane decision (`rule_based`\|`rl`) | `rule_based` |
| CHAIR_ROUTING_STRATEGY | Chair router — which chair (`intent_mapping`) | `intent_mapping` |
| QUERY_STRATEGY | Retrieval-query build (`prefix`=legacy body+intent · `distill`=one LLM call → 1-3 queries + intent) | `prefix` |
| CALIBRATION_ENABLED | Apply confidence calibration layer | `False` |
| ALLOW_AUTO_SEND | Transport gate — `True` lets complete FAQ drafts release without approval | `False` |
| CONFIDENCE_THRESHOLD | Min classifier confidence for FAQ lane | `0.75` |
| FAQ_CONFIDENCE_THRESHOLD | Min confidence router applies for FAQ auto-reply | `0.65` |
| MAX_RETRIEVED_CHUNKS | Max policy chunks retrieved | `3` |
| AL_CONFIDENCE_MARGIN / AL_EDIT_RATIO | Active-learning flag thresholds | `0.15` / `0.15` |
| DRAFTER_MAX_TOKENS | Max drafter tokens (sized for reasoning models — too low returns an empty draft ⇒ `DRAFT_TRUNCATED`) | `2500` |
| DRAFT_MODEL | Drafter model id (never hardcode in source; read from here) | `claude-sonnet-5` |
| LOCAL_MODEL_BASE_URL / LOCAL_MODEL_NAME | Self-hosted OpenAI-compatible endpoint + model | `http://localhost:11434/v1` / `llama3.1:8b` |
| LOCAL_MODEL_API_KEY | Optional bearer token for a hosted keyed chat-completions endpoint | `None` |
| STYLE_GUIDE_PATH | Reply style guide appended to the drafter system prompt (backend/-relative) | `../data/style_guide/style_guide_v2.md` |
| FAISS_MODEL_NAME | Embedding model for FAISS | `all-MiniLM-L6-v2` |
| ANTHROPIC_API_KEY | Cloud API secret | `None` |
| DATABASE_URL | Async DB URL (SQLite→aiosqlite; Postgres+asyncpg passthrough) | `sqlite:///./conference_email.db` |

## Database Tables (`backend/app/db/models.py`)
Pipeline outputs (classification, routing, draft) stored as JSON columns on `emails`.
Full ERD + JSON-column semantics (incl. `draft.history[]`, `retrieval_context`) and deprecation markers: **`docs/database-schema.md`** — hand-maintained from live introspection; **regenerate after any schema-changing migration**.
| Table | Model | Purpose |
|---|---|---|
| emails | Email | Incoming email/ticket + lifecycle (status, classification/routing/draft JSON; `assigned_chair_id` FK→chairs; Zendesk fields `source`, `zendesk_ticket_id` [unique], `zendesk_requester_id`, `zendesk_status`, `zendesk_created_at`, `zendesk_updated_at`, `last_processed_comment_id`) |
| email_thread_messages | EmailThreadMessage | Child rows per Zendesk comment (Piece 3): `email_id` FK→emails (CASCADE), `zendesk_comment_id` [unique], `public` bool, `author_id`, `author_role`, `plain_body`, `html_body`, `created_at` [thread order], `via_channel`, `ingested_at`. Initial inquiry = first `public` end-user message by `created_at` (derived by query) |
| zendesk_sync_state | ZendeskSyncState | Poller checkpoint (Piece 4): one row per account (`subdomain` [unique]), `cursor` (incremental resume; NULL→`start_time`), `start_time`, `last_synced_at`, `last_error`, `tickets_seen`, + overlap-guard `is_running`/`running_since` (single-flight lock, `FOR UPDATE` + 900s staleness takeover). DB-persisted so polling survives restarts / is row-lockable for multi-instance |
| audit_logs | AuditLog | Append-only actions (actor, action, `timestamp`, `extra_metadata` [DB col "metadata"]) |
| policy_documents | PolicyDocument | Policy KB entries (policy_key, title, content, category, score, `tags` JSON + `source`) |
| chairs | Chair | Conference chairs for assignment (name, role_title, `areas` JSON, active); empty areas = fallback |

Migrations (`cd backend && alembic upgrade head`; env `backend/migrations/`):
`988d40d1a9ee_initial` → `507ef4c2d805_phase3c_postgres_ready` → `1f51f0224943_phase6a_chairs` (chairs + emails.assigned_chair_id + 5 seeded chairs) → `b8d3f6a1c204_phase_e_policy_tags_source` (policy tags + source) → `f1a2b3c4d5e6_phase_f_policy_kb_layers` → `a7b8c9d0e1f2_phase_f_policy_audit`, which then **branches**: (Zendesk) `→ d2e4f6a8b0c1_zendesk_ticket_schema → e3f5a7c9b1d2_zendesk_sync_state`; (incoming redraft) `→ c1d2e3f4a5b6_phase_g_email_redraft`. The two branches **merge** at `c361c1d3ad79` (redrafting + Zendesk) → `f7a1b2c3d4e5_zendesk_sync_overlap_lock` (`is_running`/`running_since`) → `e7a9c1f2b3d4_drop_policy_tags` (E007: drops `policy_documents.tags`; down re-adds nullable JSON) → `a1b2c3d4e5f6` (intents JSON column on `policy_documents`) → `b2c3d4e5f6a7` (re-seed `chairs.areas` to the 14-intent families; **code head**). **Demo Postgres is at `f7a1b2c3d4e5`** — applied 2026-07-19 as a side effect of a frontend rebuild recreating the backend container (startup auto-runs `alembic upgrade head`); benign/additive columns, 47 toy + 6 zendesk rows intact, no data loss (the prior review gate at `c361c1d3ad79` is therefore closed). `e7a9c1f2b3d4` (from `kb_enhancement`) is applied to dev SQLite; **not yet applied to demo Postgres**.

## Folder Structure
```
conference-email-system/{CLAUDE.md, README.md, LICENSE, *.pdf}
data/emails/toy_dataset.json (30) · data/knowledge_base/policies.json (93 real AAAI-27 chunks) · data/eval/ground_truth.json
data/policy_corpus_real/*.md (6 source docs) · data/style_guide/{style_guide_v1.md, style_guide_v2.md, manifest.json}
archive/ (56-chunk corpus track, superseded by 7C) · scripts/generate_progress_pdf.py
backend/  pyproject.toml · alembic.ini · main.py (FastAPI root, /health, /api/v1/health/model)
  migrations/{env.py, versions/} · scripts/ (seed, run_eval, chunk_policies, bench_real, distiller/eval tooling) · reports/ · models/ · tests/ (conftest hermetic)
  app/core/{config.py, tracing.py, events.py, send_gate.py} · app/db/{database.py, models.py} · app/models/{enums.py, schemas.py}
  app/repositories/{email,policy,audit,chair}_repository.py
  app/pipeline/{classifier, retriever, faiss_retriever, fusion_retriever, router, rl_router, chair_router, drafter, template_drafter, trainable_classifier, calibration, active_learning, distiller, orchestrator}.py
  app/api/routes/{emails,dashboard,auto_replies,audit,training}.py · app/api/v1/{emails,analytics,retrieval}.py
  app/integrations/zendesk/{credential_provider, adapter, sender}.py
frontend/  package.json (Next.js 14.2.35) · src/{app/, components/, lib/, hooks/, types/index.ts}
docs/{PIPELINE_AUDIT.md, ZENDESK_API.md, DRAFTER_ADAPTER_SPEC.md, exp_tracking/E001-E003}
```

## Ticket Direct-Links & Shareable URLs (Piece B-ticket-lookup)
`/tickets/{ticketId}` is a standalone, shareable page for one ticket — the **same** detail view as the queue, opened directly by Zendesk ticket number (Zendesk-style deep link). It resolves the email via a **dedicated backend lookup**, not a queue-array search, so a ticket URL works even when that row isn't on the current queue page.
- **Backend endpoint** — `GET /api/v1/emails/by-ticket/{ticket_id}` (`emails.py::get_email_by_ticket` → `EmailRepository.get_email_by_zendesk_ticket_id`). `ticket_id` is typed `int`. **Found** → `{email, audit_trail}` (byte-identical shape to `GET /emails/{email_id}`, same `_email_to_dict`/`_audit_to_dict`). **No match** (unknown id, or a row whose `zendesk_ticket_id` IS NULL / non-Zendesk) → **404** `{"detail": "No email found for ticket id {id}"}`. **Non-numeric** path segment → **422** (FastAPI, before the handler). Declared **before** the `/{email_id}` catch-all (two-segment static path — order is load-bearing).
- **Shareable-URL convention** — `${window.location.origin}/tickets/{zendesk_ticket_id}`, built from the browser origin (**never hardcode localhost/a domain**), so links are correct in every environment. The "Copy link" button in `EmailDetail.tsx` uses this. **Use this same `{origin}/tickets/{id}` pattern for any future shareable-link feature.**
- **One shared implementation** — `/queue` and `/tickets/[ticketId]` both render the single `EmailWorkspace` (`frontend/src/components/email/EmailWorkspace.tsx`): filter sidebar, drag-resize, list, detail pane, and all chair actions (approve→send→advance, reroute, reassign, retry) live there. **Change filters/layout/detail once, both routes get it — no duplicate implementations, no drift** (a `shared-workspace-identity` test guards against a fork). Selection is the only per-route difference: `/queue` uses `selectionMode="id"` with a constant `null` (empty "select an email" landing — no auto-redirect; a row click is `router.push('/tickets/{ticketId}')`, not local state); `/tickets/[ticketId]` uses `selectionMode="email"`, fetched via `useEmailByTicket` → `getEmailByTicketId` (client fn in `lib/api/emails.ts`). Its query key `["emailByTicket", ticketId]` is invalidated by every email action, so the detail refreshes immediately (not on the 15s poll).

## Testing Policy
Every pipeline module has a test file. Tests run without real DB/API (mock both, or in-memory SQLite via StaticPool + ASGITransport). A hermetic autouse conftest fixture forces `MODEL_PROVIDER=fallback` / no key / `QUERY_STRATEGY=prefix` so the suite never hits a hosted model. Fast iteration: `-m "not ml"` (217 passed, 6 skipped) skips embedding-heavy tests; full suite = 246 tests collected (adds 23 embedding-heavy `ml` tests). `cd backend && python -m pytest tests/ -v`

## Engineering Rules
Always: read existing code first; keep modules separate + typed; DB access via repositories; test every pipeline module; update this file at end of session.
Never: mix frontend/backend logic; hardcode model names in source (use `DRAFT_MODEL`/`LOCAL_MODEL_NAME`); create monolithic files; skip the CLAUDE.md update.
DB note: `main` defaults to a local SQLite file (created under `backend/` at the process CWD). Seed/migrate via `cd backend && python scripts/...`.

## How to Run
```
cd backend && pip install -e . && alembic upgrade head && uvicorn main:app --reload
cd frontend && npm install && npm run dev
cd backend && python scripts/seed.py            # seed DB
cd backend && python -m pytest tests/ -v         # tests
cd backend && python scripts/run_eval.py         # eval harness
python scripts/generate_progress_pdf.py          # progress PDF
```

---

## Phase History

### Phases 0–4 — Foundation → Intelligence — Complete
- **0 Foundation**: FastAPI scaffold (main.py, config.py Settings+get_settings(), db async engine/session/Base + models, Pydantic v2 schemas), Alembic initial migration, Next.js 14 shell. /health 200.
- **1 Data+Pipeline+API**: 30 labeled toy emails; repositories (Email/Policy/Audit, async, reads never raise); flat pipeline modules (classifier keyword-overlap, retriever BM25, router sensitive-override+FAQ gate, drafter provider-aware w/ fallbacks, orchestrator classify→retrieve→route→draft→persist+audit); v1 API (ingest/queue/{id}/approve/reroute + analytics); seed.py.
- **2 Frontend**: API client + React Query hooks; dark indigo design system; pages /dashboard /queue (split-pane review) /auto-replies /audit /analytics (recharts).
- **3 Hardening** (config-flag swaps, defaults unchanged): 3E audit endpoint · 3C PostgreSQL-ready (asyncpg/psycopg2 deps, checkpoint migration; SQLite still default) · 3D local drafter (`_draft_local`, /health/model) · 3A trainable classifier (MiniLM+LogReg, `CLASSIFIER_BACKEND`) · 3B RL router (epsilon-greedy bandit, `ROUTING_STRATEGY=rl`).
- **4**: 4A FAISS retrieval (`get_retriever()` factory, `RETRIEVAL_BACKEND=faiss`) · 4B eval harness (per-intent P/R/F1, routing acc, retrieval hit-rate) · 4C progress PDF.

### Phase 5 — Eval, Observability, Fusion, Drafters, Review UX — Complete
- **5A**: per-email tracing (`app/core/tracing.py` → logs/pipeline_trace.jsonl, `/emails/{id}/trace`) · retrieval-only metrics (recall@k/nDCG@k) · eval set boundary cases.
- **5B**: confidence calibration (`calibration.py`, platt/isotonic, `CALIBRATION_ENABLED` opt-in, off by default; big routing win when on, 0 over-promotions).
- **5C**: RRF fusion retriever (`fusion_retriever.py`, bm25+faiss, k=60, `RETRIEVAL_BACKEND=fusion`) — on the toy corpus fusion sat between bm25 and faiss (default stayed bm25).
- **5D**: template drafter (`template_drafter.py`, `MODEL_PROVIDER=template`, zero model call, verbatim-grounded) — safest fallback.
- **5E**: live queue via SSE (`events.py` EventBroker, `/emails/stream`) · calibration reliability diagram on /analytics.
- **5F**: chair-edit diff (original preserved in draft JSON + both texts in audit, word-level diff) · A/E/R keyboard shortcuts.
- **5G**: active-learning flagging (`active_learning.py`, near-miss confidence + meaningful edit → two distinct audit actions + candidates endpoint + /analytics card; review-list only, no auto-retrain).
- **5H**: drafter adapter spec (`docs/DRAFTER_ADAPTER_SPEC.md`, zero model names).
- **5I**: Docker Compose (backend+frontend, **SQLite volume**) + secret-free GitHub Actions CI (tests · eval artifact · tsc). Live-verified /health 200 + /dashboard 200.

### Phase 6 — Multi-Chair Routing — Complete
- **6A**: second routing decision ("which chair"), separate from the lane router. Chair table + `Email.assigned_chair_id` FK (5 seeded chairs). Classifier taxonomy **8→11 intents** (+sponsorship, publicity, media_inquiry). `app/pipeline/chair_router.py` (ChairRoutingStrategy ABC + IntentMappingStrategy + `CHAIR_ROUTING_STRATEGY`) wired into orchestrator (human_review only, best-effort, `chair_assigned` audit). PATCH `/emails/{id}/reassign-chair` + `chair_reassigned` audit.
- **6B**: multi-chair frontend (`GET /api/v1/chairs` the only backend add). Chair badge + assigned-chair filter on queue · reassign picker (optimistic, `C` shortcut) · routing-rationale panel · 2 analytics charts.
- **6C**: fixed a **bug class** — surfaces computing counts/aggregates client-side from the capped `/queue` page dropped out-of-window rows. Backend aggregates on `analytics_summary` (chair_distribution · confidence_distribution · faq_avg_confidence · reassignment_by_chair) · `/queue` full server-side filtering + filter-scoped total. Regression tests seeded outside the page window.

### Real-Corpus + Phase 7 — Real AAAI-27 Corpus, Distiller, Placeholder Contract, Send Gate — Complete
Jiacheng's track, now on `main`. Zero model names in code/docs (data quotes AAAI's own policy verbatim).
- **Real corpus (7C unification)**: `data/knowledge_base/policies.json` is the canonical **93-chunk** real AAAI-27 corpus (`policy_101`–`193`, subsection-level, contextual titles) chunked from 6 official markdown docs (`scripts/chunk_policies.py`). The earlier 56-chunk track (`policy_046`–`101`) is superseded and moved to `archive/`. Migration `b8d3f6a1c204` added `policy_documents.tags`/`source` (FAISS↔BM25 tag parity); both indexes rebuild clean on the 93 chunks. Retriever-only; classifier/router unchanged.
- **Query distillation (E003)**: `app/pipeline/distiller.py` — one LLM call rewrites the email into 1-3 compact policy-vocabulary retrieval queries **and** classifies intent (`method="llm_distiller"`), gated by `QUERY_STRATEGY=distill` (default `prefix` = legacy bit-for-bit). On any failure → keyword classifier + subject+body[:600] query. Real-ticket ablation: distilled-joined hit@3 **.892 vs .649**. Deploy recipe: `QUERY_STRATEGY=distill` + `RETRIEVAL_BACKEND=fusion`.
- **Placeholder reply contract (7F)**: drafter emits structured REPLY/CITATIONS/NOTES FOR CHAIR; chair-facing gaps become inline `[CHAIR: …]` placeholders + `notes_for_chair` (never in the reply body). Deterministic enforcement: orchestrator forces human_review when placeholders exist; approve endpoint 409s while `[CHAIR: …]` remains; leak detector flags residual meta language (flag, never rewrite). Internal `policy_NNN` ids scrubbed from requester text. Real-ticket leak rate **86% → 0%**.
- **Send gate**: `app/core/send_gate.py` `authorize_send()` + `POST /emails/{id}/send` — single precondition for any future transport. Default (`ALLOW_AUTO_SEND=False`) only status "approved" is sendable regardless of lane; unresolved placeholders block even an approved email; both outcomes audited; no transport yet → authorized send returns 501, draft stays queued.
- **Style guide**: distilled from real chair replies (v1) + curated **v2** (adopted after blinded A/B). Appended to the drafter system prompt (contract in the fixed prompt, voice in the guide) via `STYLE_GUIDE_PATH` — see today's entry.
- **Test infra**: hermetic autouse conftest fixture (fallback provider / no key / prefix) cut the suite from ~7.5 min to seconds; `ml` marker on embedding-heavy modules. Zendesk groundwork (`docs/ZENDESK_API.md`, read-only OAuth + pull script) — no poller/write-back yet.
- Findings live in `docs/PIPELINE_AUDIT.md` + `docs/exp_tracking/E001-E003`. Coverage on real traffic ~18.3% (human-review lane is the product); classifier real-intent accuracy 57.8% (fine for chair routing, not as an FAQ gate).

### Today (2026-07-17) — style_guide_v2 made the committed default — Complete
- `STYLE_GUIDE_PATH` default `None` → `../data/style_guide/style_guide_v2.md` (config.py + .env.example), commit **`c4ed3f5`**. Uses the backend/-relative `../data` form so it resolves from the app CWD (Docker WORKDIR /app/backend, local `cd backend`, pytest rootdir backend/) — a bare `data/...` would silently no-op. Drafter loader / v1 / manifest untouched. Verified: default loads the real 2789-char v2 guide into the system prompt; **184/184 tests pass**.
- **Minor future cleanup (flagged, not done)**: `test_no_style_guide_by_default` in `test_drafter_local.py` is now a mild misnomer — it monkeypatches `STYLE_GUIDE_PATH=None` (testing the explicit-None override), which is still valid and passing, but the default is no longer None. Rename later (keep the intent), low priority.

### 2026-07-17 (later) — SQLite→PostgreSQL migration — Complete · now on main
Infra + data only; six pipeline modules untouched. Overview in the **Current Status table** ("PG migration" row), **Branch/State note**, and **Migrations** reference. Load-bearing specifics: `json_extract` → dialect-agnostic accessors at exactly two sites — `email_repository._queue_conditions` → `Email.routing["lane"].as_string()`; `audit_repository.count_reassignments_by_original_chair` → `AuditLog.extra_metadata["original_chair_id"].as_integer()` (SQLite-only func; two regression tests in `test_postgres_migration.py` guard reverts). PG test fixture: schema once/module via psycopg2 (sync, no event loop) + async engine/session per test (avoids cross-loop asyncpg errors); suite gated on `TEST_DATABASE_URL` only (never a real DB).

### 2026-07-19 — Re-evaluate open tickets on policy change — Merged to main 2026-07-19 (from `feature/reevaluate-on-policy-change`)
Chair edits the KB, clicks **"Re-evaluate open tickets"** on `/knowledge-base` → `POST /api/v1/policies/reevaluate` schedules ONE background sweep (`app/pipeline/reevaluation.py`). Sweep re-runs retrieval for each open ticket (DRAFT_GENERATED) using the query+intent captured at ingest — **no model call** — and re-drafts only tickets whose top-k policy **set** changed; live "re-drafting…" badge via SSE. Schema: `emails.retrieval_context` (JSON) + `emails.redrafting` (bool), migration `c1d2e3f4a5b6`. Hardened per review: skip NULL-context legacy rows; atomic `claim_for_redraft` + status-conditional `save_redraft` (no double-draft / no approve-during-sweep clobber); `clear_stale_redrafting_flags` at startup. Follow-up (design §9): startup-clear is single-worker-scoped. **211 non-ml tests pass, frontend tsc clean.** Built via SDD; design/plan under `docs/` (plans dir gitignored). Also carries two drafter prompt commits: trim four system-prompt rules; answer-only-what's-asked scope fix (over-answering).

### 2026-07-19 — Retrieval rework: embed leaf title (E005) — Merged to main 2026-07-19 (from `retrieval-rework`)
KB-rework audit (`docs/local/RETRIEVAL_REWORK.md`) found the dense corpus barely separable —
82% of chunks had a >0.7 near-twin, almost all intra-document — because the repeated
`<Doc> — ` title prefix is embedded into every sibling chunk. **E005**
(`docs/exp_tracking/E005_embed_representation.md`, harness `scripts/e005_embed_repr.py`
on the 37 real-gold tickets, distilled+fusion) confirmed dropping the prefix from the
*embedded* string helps: dense hit@1 .514→.649 / MRR .665→.756; production fusion hit@1
.649→.703, gold rank 2.3→2.1, no regression; leaf beat content-only. Shipped:
`faiss_retriever.py` `_embed_text`/`_leaf_title` (stored `title` unchanged → BM25 +
citations keep the full path; dense-embed-only). Tests +2. Doc-design menu (Ideas A–H,
tags/intent/chunking audit) in `docs/local/RETRIEVAL_REWORK.md`; next: dedup cross-doc twins,
question-gen multi-vector index (E006).

### 2026-07-19 — Zendesk integration Pieces 1–5 + live e2e + overlap guard — COMPLETE
Full read+write Zendesk integration, no pipeline module touched (adapters CALL the pipeline). Blow-by-blow lives in the **Current Status table** (Pieces 1–3/4/5/overlap-guard rows), **Database Tables** (email_thread_messages, zendesk_sync_state — incl. the single-flight lock + "initial inquiry = first public end-user msg" rule), **Open Blockers** (operational gating), and **Migrations** (d2e4f6a8b0c1→e3f5a7c9b1d2→f7a1b2c3d4e5, applied to demo Postgres). Modules under app/integrations/zendesk/ (see Folder Structure): credential_provider.py (ZendeskCredentialProvider ABC + Token/OAuth, factory on ZENDESK_AUTH_MODE), adapter.py (ZendeskIngestAdapter.sync — cursor export, upsert, classify-once), sender.py (ZendeskSender transport — add_comment/add_tags, internal-note default, public reply gated on ALLOW_AUTO_SEND+public=true, tags ai_drafted/ai_auto_replied). First live e2e: internal-note write-back to ticket 21567.

### 2026-07-19 (later) — Queue status bar + self-hiding source toggle — Complete
Queue UX for the mixed toy_dataset/Zendesk demo era. No pipeline module touched (classifier/retriever/router/drafter/orchestrator unchanged); persistence via the repository only, following the Phase 6C dedicated-aggregate rule (never a client tally over a capped page).
- **Backend — new filters**: `emails.queue` gains server-side `source` + `zendesk_status` exact-match filters (added to `EmailRepository._queue_conditions` / `get_email_queue` / `count_email_queue`, both new params default `None`). `_email_to_dict` now exposes `source` + `zendesk_status` so queue rows can be badged/gated client-side. These COMPOSE with the existing lane/chair/unassigned/status/search filters (shared `_queue_conditions`).
- **Backend — dedicated facets aggregate**: `EmailRepository.count_queue_facets` (three grouped queries) → `GET /api/v1/emails/queue/facets`. Response `{by_zendesk_status, by_source, sources}`. Context filters (lane/chair/unassigned/status/search) honored so the bar/toggle counts compose; the facet dimensions themselves (source, zendesk_status) are deliberately NOT applied (selecting a status must not zero the others). `by_zendesk_status` is scoped to `source='zendesk'`; `sources` is the DISTINCT non-null source list over the WHOLE table (unfiltered) — the single source of truth for the self-hide decision. Route registered before `/{email_id}` (two-segment path, no shadowing).
- **Frontend — components** (`components/email/`): `ZendeskStatusBar` (compact clickable rows, label + right-aligned count + status-colored dot, canonical order new→open→pending→hold→solved→closed, click-active-to-clear + Clear control) and `SourceToggle` (All / Zendesk / Toy Dataset segmented control that **renders nothing when `sources.length < 2`** — so once toy_dataset is retired the toggle vanishes with zero cleanup). Both pure/presentational, styled with the existing dark CSS-var + indigo-accent system. Wired into the queue left pane via new `sourceFilter`/`zendeskStatusFilter` state + `useQueueFacets(contextParams)` hook; status bar shown only when `sourceFilter !== 'toy_dataset'` and Zendesk counts exist; switching source→toy_dataset clears any zendesk_status filter.
- **Frontend test infra (NEW)**: introduced Vitest + React Testing Library (`vitest.config.ts` jsdom/`@` alias, `vitest.setup.ts` jest-dom + cleanup, `npm test`/`test:watch`) — the project's first frontend tests. 11 component tests (SourceToggle self-hide/show/click/aria ·5; ZendeskStatusBar counts/order/empty/select/clear/aria ·6).
- **Tests**: backend **274 passed / 6 skipped** (`-m "not ml"`; +9 new `tests/test_queue_facets.py` — facet counts over out-of-window rows, by_source, distinct sources, lane-compose, single-source self-hide, source/zendesk_status queue filters, zendesk_status×lane composition, `_email_to_dict` exposure). Frontend **11 vitest passed**, **tsc clean**.
- **Live verify + stale-build fix (2026-07-19)**: the components didn't appear in the running demo app at first — diagnosed as a **stale frontend image** (the `frontend` compose service bakes a production `next build` at image time, no source volume mount; running image predated the feature; `.next` bundle grep for `Zendesk Status`/`Filter by source` was empty). Fixed with `docker compose up -d --build frontend` — new bundle contains both markers, `GET /api/v1/emails/queue/facets` live returns `{by_source:{toy_dataset:47,zendesk:6}, sources:[toy_dataset,zendesk], by_zendesk_status:{new,closed}}`, so both toggle (≥2 sources) and status bar render. **Side effect**: the rebuild recreated the backend container, whose startup `alembic upgrade head` **applied migration `f7a1b2c3d4e5`** (overlap-lock columns) to the demo Postgres — additive only, 47+6 rows intact, no data loss; demo now at head `f7a1b2c3d4e5` (prior review gate closed). Feature files remain **uncommitted** in the working tree (Docker built them from the working-tree context, not git).

### 2026-07-19 (later) — Drop policy tags (E007) — Complete on `kb_enhancement`
E007 ablation (`docs/exp_tracking/E007_policy_tag_ablation.md`, harness `scripts/e007_tag_ablation.py`): the auto-generated policy `tags` carry **no measurable retrieval signal** — stripping them from the BM25 doc string is flat-to-neutral at both BM25 and production fusion (every Δ ≥ 0; largest +.027 = 1 ticket at n=37, within ±.05 noise; `tags_on|fusion` reproduces E005's `leaf|fusion` exactly). Dense held at prod leaf-title; only the BM25 corpus was swapped tags-on/off.
- **Dropped tags:** migration `e7a9c1f2b3d4` drops `policy_documents.tags` (down re-adds nullable JSON). The app-side tag path is **commented out, not deleted** (marker `[tags-dropped E007]`) so a future controlled facet vocab can restore it: `retriever`/`faiss_retriever`/`fusion_retriever`, `policy_repository` (importer fields + `create_internal`), `/policies` API (request/response + hydration), `PolicyDocument` model, `chunk_policies.py`, frontend (`AddPolicyPanel` input, `PolicyDetailModal` badges, `types/index.ts`). `RetrievedChunk.tags` DTO + Fusion's tag-merge left inert (operate on empty lists).
- **Tests:** suite green **270 passed / 7 skipped** (null-tag-coercion endpoint test skipped; tag assertions in retriever/endpoint/kb-layers/postgres-migration updated). Frontend `tsc --noEmit` clean. Migration applied to dev SQLite — column verified gone.

### 2026-07-20 — Intent taxonomy adoption + FAQ-lane rework (draft-quality gate) — Merged to main 2026-07-20 (from `feature/intent-taxonomy-adoption`; reconciled with Sahil's queue-facets/Zendesk-status-bar work — no conflicts, single alembic head)
Two stacked bodies of work on one branch off `main`. Built via SDD (per-task + whole-branch review). Zero model/vendor names in tracked source/docs.
- **Intent taxonomy adoption** (base, commits `705a38b..2cc2577`): `app/pipeline/taxonomy.py` is the single source of truth for **14 intents / 5 families** (data-mined from ~18.5k tickets, chair-tag-validated, year-persistent — replaced the hand-crafted 11). Classifier / distiller / router / template drafter / frontend all repointed; dead `EmailIntent` enum removed. `intents` JSON column on `policy_documents` (mig `a1b2c3d4e5f6`); 93 KB chunks LLM-labeled → coverage map (`backend/reports/kb_intent_coverage.json`). A soft intent-retrieval prior was built but **E010** (`docs/exp_tracking/E010_intent_prior.md`) showed it badly regresses fusion retrieval (hit@1 .730→.243) → **gated OFF** (`INTENT_PRIOR_ENABLED=False`, infra kept). Taxonomy doc: `docs/INTENT_TAXONOMY.md`.
- **FAQ-lane rework** (commits `c3a3492..a4ab958`, `docs/exp_tracking/E011_faq_lane_rework.md`): the FAQ (auto-answer) lane is now a property of the generated **draft**, not the email's intent. The drafter self-rates `answer_confidence` (LLM only; non-LLM → `None`). The router runs **AFTER** the drafter (`route(classification, retrieved_chunks, draft)`); FAQ requires ALL of: no `[CHAIR:…]` placeholders · no notes-for-chair · grounded (cites ≥1) · classifier confidence ≥ `FAQ_CONFIDENCE_THRESHOLD` · `answer_confidence` ≥ new `FAQ_ANSWER_CONFIDENCE_THRESHOLD` (0.85) — any failure → human_review with a specific reason. `FAQ_ELIGIBLE_INTENTS` whitelist **retired**; `SENSITIVE_INTENTS=[]` (seam kept — appeals are answerable, a complete grounded "no" is auto-eligible). Strategy-independent `router.apply_self_sufficiency_floor(routing, draft)` demotes any placeholder / notes / unrated / ungrounded draft to human_review regardless of routing strategy (covers the dormant RL path, which routes before a draft exists). `chairs.areas` re-seeded to the 14-intent families (mig `b2c3d4e5f6a7`, single head). `scripts/run_eval.py` now routes on a real draft (routing_accuracy is end-to-end / provider-dependent). Non-LLM `answer_confidence=None` → never FAQ (fail-safe); auto-send stays off; RL routing out of scope. Whole-branch review: **READY TO MERGE**. Full suite **325 passed / 7 skipped** (incl ml, 0 failed); non-ml **294 / 7**.

### 2026-07-20 (later) — Pre-deployment `.env.example` cleanup (docs-only; NOT committed — Sahil to commit) — Complete
Prep for the first production deploy on AAAI's server. Documentation/cleanup only — **zero runtime change** (`config.py` untouched, no default altered). No pipeline module touched.
- **Deleted the stale root `.env.example`**: its backend section was superseded by `backend/.env.example` (outdated defaults `MODEL_PROVIDER=anthropic_api`/`RETRIEVAL_BACKEND=bm25`, the invalid value `vector`, no Zendesk/`QUERY_STRATEGY`/`ALLOW_AUTO_SEND`/polling vars). Reference audit confirmed the only live use was the **frontend** guidance (README's `cp ../.env.example .env.local`); every other `.env.example` mention (README tree/backend-setup, `test_env_example_config.py`, DRAFTER_ADAPTER_SPEC/PIPELINE_AUDIT docs) resolves to `backend/.env.example`.
- **Created `frontend/.env.example`** (frontend had none — only the gitignored `.env.local`): preserves the `NEXT_PUBLIC_API_URL` guidance, using the **correct working value** `http://localhost:8000/api/v1` (the root file's `http://localhost:8000` was missing the `/api/v1` prefix the real `.env.local` uses). Confirmed not gitignored (`.gitignore`'s `!.env.example`).
- **README.md frontend setup**: `cp ../.env.example .env.local` → `cp .env.example .env.local` (+ hint now `/api/v1`).
- **`backend/.env.example` documentation adds** (all set to the existing `False` default — visibility only): `ALLOW_AUTO_SEND=False` (new "Transport / auto-send gate" section — gates all outbound/public replies; must stay False until production sign-off) and `ZENDESK_POLLING_ENABLED=False` (new "Zendesk polling" section — gates the background poll loop; manual `POST /api/v1/zendesk/sync` still works when off; must stay False until integration fully verified). `ZENDESK_AUTH_MODE` guidance corrected to **recommend `oauth`** (the validated production path per `docs/ZENDESK_API.md`) with an explicit note that the config.py code default stays `token`.
- **Verified**: `test_env_example_config.py` still **2 passed** (`oauth` is a valid Literal member; new `False` assignments load). Working tree: `D .env.example`, `M README.md`, `M backend/.env.example`, `?? frontend/.env.example` — left uncommitted for Sahil.

### 2026-07-20 (later) — Deployment hardening: internal-only host bindings + configurable frontend API URL (3 pieces) — Complete
Prep for the first production deploy on AAAI's **shared** server — must stay **internal-only** (not public-internet exposed) for now. Infra/docs only; no pipeline module or `config.py` touched. Base `docker-compose.yml` had backend (8000) + frontend (3000) with **no host binding → 0.0.0.0** (publicly reachable); only `db` was loopback-bound (`127.0.0.1:5432`).
- **Piece 1 — `docker-compose.prod.yml` (new override file)**: rebinds backend + frontend to `127.0.0.1` for internal-only reachability. Invoked ONLY when explicitly combined: `docker compose -f docker-compose.yml -f docker-compose.prod.yml up`. Base file untouched → local dev unaffected. **Gotcha caught + fixed**: a plain `ports:` list **concatenates** across compose files (verified via `docker compose config` — the base `0.0.0.0:8000` binding survived alongside the loopback one, leaving the port public). Fixed with the **`ports: !override`** YAML tag (Compose ≥ 2.24; host is v5.1.0) so the list is REPLACED. Verified merged config = loopback-only on both app services; base-only config byte-identical to before.
- **Piece 2 — configurable frontend API URL** (in `docker-compose.yml`): `NEXT_PUBLIC_API_URL` build arg hardcoded `http://localhost:8000/api/v1` → `${NEXT_PUBLIC_API_URL:-http://localhost:8000/api/v1}` (shell/env override, same value as fallback default). Comment warns the value is **baked into the browser bundle at BUILD time** — prod must set it BEFORE `docker compose build`/`up`; a container restart alone won't pick up a change (rebuild required). Verified: no env var → unchanged localhost default; env var set → override resolves.
- **Piece 3 — README "Production Deployment" section** (after "Getting Started"): local dev (`docker compose up --build`, unchanged localhost defaults) · internal/prod deploy (set `NEXT_PUBLIC_API_URL` first, then `-f docker-compose.yml -f docker-compose.prod.yml up --build -d`) · what loopback-only means (reachable only from the server itself, e.g. SSH tunnel, **not** the public internet). **Reverse proxy / nginx / TLS deliberately out of scope** — flagged as a separate future piece.
- **State per stakeholder**: Pieces 1 & 2 **merged to main**; Piece 3 (README) built this session, uncommitted (Sahil to commit).

### 2026-07-20 (later) — Zendesk ticket-number badge + status-select approve button (Piece D) — Complete · **UI/frontend-only**
Review-UX for the Zendesk era, built in 6 pieces + a loading follow-up. **Frontend-only — no backend write to Zendesk status happens yet** (blocked on the per-chair OAuth/send endpoint; see Open Blockers).
- **`zendesk_ticket_id` exposed end-to-end**: added to `_email_to_dict` (backend, the one non-frontend change — additive serialization, no schema/model touch) + the frontend `Email` type. Null for non-Zendesk rows.
- **Ticket-number badge**: muted `#{ticket}` (tabular-nums, `#` prefix) in the list row's sender/time line (`EmailListItem`) and the detail header's sender/date line (`EmailDetail`) — null-guarded (renders nothing, no `#null`/stray separator for non-Zendesk rows).
- **`SplitActionButton`** (new reusable `components/ui/split-action-button.tsx`): shadcn `Button` + a newly-added `DropdownMenu` primitive (installed `@radix-ui/react-dropdown-menu`, hand-wrapped to the app's CSS-var tokens like `button.tsx`, since stock shadcn classes render colorless here). Primary "Approve & Send" → dropdown picks Open/Pending/Solved; once chosen, label → "Submit as {Status}" and `onAction(status)` fires the chosen status (`null` in default mode). Optional controlled-selection props + a `loading` prop (reuses `LoadingSpinner`). Generic — not bound to email context.
- **Shared status-color source of truth** (`frontend/src/lib/zendesk-status.ts`): `ZENDESK_STATUS_COLORS` + `zendeskStatusColor()` — reconciles `SplitActionButton` and `ZendeskStatusBar`, which had diverged (native red/blue/gray vs the app-harmonized palette). Adopted `ZendeskStatusBar`'s palette (already in the app's visual language); both now import it, zero literals left in either.
- **Wired into `EmailDetail`**: replaced the inline "Approve & Send" `<button>` with `SplitActionButton` (`disabled={!canApprove}` + `loading={isApproving}` — same effective gating; unresolved `[CHAIR:…]` placeholders still block); the "A" shortcut fires the same action incl. the chosen status (selection lifted into `EmailDetail` via controlled props). `target_status` (`open|pending|solved|null`) threaded through the frontend approve payload: `EmailDetail.onApprove` → `queue/page.tsx` → `approveEmail()` → `ApproveRequest` type. **Explicit `// TODO: backend does not yet consume target_status - pending per-chair OAuth send endpoint (Piece C)`** at the `approveEmail()` send site — backend `ApproveRequest`/route untouched.
- **Verified**: backend affected tests green (additive field, no snapshot assertion broke); frontend `tsc --noEmit` clean; vitest **11/11**. (14 pre-existing non-ml backend failures are a stale dev-SQLite `policy_documents.intents` env issue, reproduce on pristine `main`, unrelated.)

### 2026-07-21 — Zendesk status filtering (Piece A, A1–A6) — Complete · verified live end-to-end
Two-layer status filtering: fetch-time (skip on ingest) + queue-time (UI filter/facet). No pipeline module touched (adapter/repo/API/frontend only).
- **Fetch-time design decision — client-side skip, NOT Zendesk's Search API**: the incremental cursor export has no server-side status filter, so filtering happens client-side in the adapter. Search API was **rejected**: it isn't built for reliable delta-sync (indexing lag → missed/duplicated tickets) and switching to it would **break the incremental-cursor resume guarantee** the poller depends on. The skip runs in `_process_ticket` **before the per-ticket comment fetch**, so filtered statuses cost zero comment-API budget. `deleted` is still always skipped (never a valid allow-list value).
- **`ZENDESK_SYNC_STATUSES` config flag (A2a) + per-call override (A2c)**: comma-separated allow-list, default = all ingestable statuses (`new,open,pending,hold,solved,closed`) so unset behavior is unchanged. Parsing single-sourced in `config.parse_zendesk_statuses()` (trim/lowercase/dedupe, validate against `Settings.ZENDESK_VALID_STATUSES`, unknown tokens dropped, empty→all). `ZENDESK_VALID_STATUSES` ClassVar excludes `deleted`. `POST /api/v1/zendesk/sync` gains an optional `statuses` query param (A2c) that overrides the config for **that call only** — threaded `endpoint → run_sync_cycle → adapter.sync(statuses=…)`; polling and unparameterized calls keep the config default (poll loop passes no `statuses`). Adapter gains `SyncResult.skipped_status` counter. **NOTE: A2b (adapter skip logic) was found NOT actually present in the tree despite the task assuming it — implemented as part of A2c so the override is functional.**
- **Solved/closed bucketing (A3)**: one queue filter/facet value. `_SOLVED_BUCKET_ALIAS="solved"` / `_SOLVED_BUCKET_STATUSES=("solved","closed")` in `email_repository.py`. **"solved" was REUSED as the alias** (not a new `"solved_or_closed"` value) so the frontend's existing "Solved" row needed **zero new value plumbing**. `_queue_conditions`: `zendesk_status="solved"` → `IN ("solved","closed")`; every other status stays exact-match. `count_queue_facets` folds `closed` into the `solved` key (Python, post-query — keeps the grouped query dialect-simple; handles closed-only case). A solved+closed ticket auto-transitions solved→closed with no further chair action, so they are one operational bucket.
- **Frontend (A4)**: `ZendeskStatusBar` `STATUS_META` "Solved" row relabeled **"Solved / Closed"**; the standalone **"Closed" row removed — intentionally dead** (backend never returns a standalone `closed` facet, so it was unreachable). Click still emits `zendesk_status="solved"` (the alias). `zendesk-status.ts` keeps the `closed` color (still used by `SplitActionButton`). +2 vitest.
- **Verified live end-to-end (A5/A6), not just unit tests**: freshly rebuilt Docker images (backend image `_SOLVED_BUCKET_ALIAS` grep=4, frontend bundle `"Solved / Closed"` ×56 — proven baked in, not just source). Against **real synced demo data** (Postgres: solved=10, closed=5, new=1, toy=47): (1) `?zendesk_status=solved` → **total 15 = 10 solved + 5 closed**; (2) `?zendesk_status=open` → total 0, zero leak (exact-match intact); (3) facets → `{solved:15, new:1}`, no standalone `closed` key. Backend 35 queue/facet tests + adapter/config tests green; frontend 13 vitest, tsc clean. Also fixed an unrelated pre-existing `react/display-name` lint error in `useEmailThread.test.tsx` (T3b) that was failing the production `next build`. **Env caveat**: a VS Code/WSL dev server holds host `127.0.0.1:8000` — a browser demo is correct only when `localhost` resolves to the docker backend (IPv6 `::1`); stop that dev server for an unambiguous browser test (verification above hit the docker backend directly).
- **⚠️ CLAUDE.md compression now overdue**: this file is **325 lines** (was already over target after Piece D). The Phase History's older, fully-superseded entries (early Zendesk Pieces 1–5 blow-by-blow, PG-migration deviations) should be compressed to one-liners next session.

### 2026-07-21 (later) — Multi-turn conversation threads + follow-up reprocessing — Merged to main 2026-07-21 (from `feature/multi-turn-threads-followup`; built via SDD)
Surfaces the already-stored `email_thread_messages` and treats a ticket as a multi-turn conversation end-to-end. **No migration** (draft history is a `history[]` list inside the existing `Email.draft` JSON; audit reuses `audit_logs`). Built + reviewed via SDD (8 tasks, per-task + final opus whole-branch review → ready-to-merge).
- **Surface + UI**: `EmailRepository.get_thread_messages` → `GET /emails/{id}/thread` (flat oldest-first messages incl. internal notes) → FE `useEmailThread` + new `ConversationThread.tsx` (Requester/Support/**Internal note** bubbles, `plain_body`, `text-base`, single scroll — replaces the old 256px monospace `max-h-64` box). "Previous drafts (N)" collapsible (default collapsed) reads `draft.history`.
- **Classification over the thread**: `app/pipeline/thread_transcript.py` `build_transcript` (internal notes excluded, latest requester turn anchored, recent-first, `THREAD_TRANSCRIPT_MAX_CHARS=16000`) → `EmailDistiller.distill(subject, body, *, transcript=None)` + `_build_user_prompt` conversation branch (single choke point; template provider skips it). Single-message path byte-for-byte unchanged.
- **Follow-up reprocess**: on a NEW public end-user comment to a **non-`closed`** ticket, `adapter._reprocess_on_followup` → `orchestrator.reprocess_email_with_thread` re-runs classify→retrieve→draft over the thread, pushes the prior draft into `draft.history[]` (`_append_draft_history`), status→`DRAFT_GENERATED`, audit `reprocessed_on_followup`. `closed` = surface-only (`customer_reply_received`; a reply spawns a new ticket). `solved` reprocesses (reopens). `SyncResult.reprocessed` counter.
- **⚠️ RECONCILIATION with Sahil's parallel follow-up work (per Jiacheng's decision):** Sahil's `EmailProcessingResult` per-message table (mig chain → head `af44a007c60f`), `compute()` seam, `add_processing_result`/`get_email_with_thread` repo reads, `_process_followup_messages`, and `ZENDESK_SYNC_STATUSES`/solved-closed bucketing (A1–A6) are all **MERGED and RETAINED, but the follow-up-population path is DORMANT** — the poller runs OUR reprocess path, not `_process_followup_messages` (kept in code, not wired into `_process_ticket`). Sahil's duplicate T3b `GET /thread` endpoint + response models + T4 frontend types/hook/tests were **superseded by ours** (removed) to avoid duplicate routes/identifiers; his A3/A4 status-bucketing + `get_email_with_thread` (dormant) kept. To reactivate his path later: re-wire `_process_followup_messages` in the adapter follow-up branch and re-add his thread view. Merge = FF `214f132..6777159`; **351 non-ml passed/7 skipped, tsc clean, single head `af44a007c60f`.**

### 2026-07-21 (later) — Workstream A: resolved-ticket guard on the policy sweep (A2-impl-1…4) — Complete · commit `a367c0c` · verified live on demo Postgres
Discovery pass over **all 6 pipeline-triggering paths** (startup · policy sweep · Zendesk sync ingest · follow-up reprocess · manual redraft · `/ingest`) asking which skip resolved Zendesk tickets. Only one needed a fix.
- **The bug (area 2)**: `reevaluate_open_tickets` → `EmailRepository.get_open_tickets` selected on **`Email.status == DRAFT_GENERATED` alone** — it never read `zendesk_status`. The two are orthogonal, so a ticket already `solved`/`closed` in Zendesk but still `draft_generated` locally was re-drafted on **every** policy edit: real model spend on a conversation nobody will read.
- **Confirmed in the wild, not theoretical**: audit trail on demo email **205** (`zendesk_status='solved'`) shows `ticket_redrafting` → `ticket_redrafted` by actor `reevaluation` at **2026-07-20 16:55**, before the fix. Its `updated_at` still matches that timestamp.
- **The fix** (`email_repository.py`, `get_open_tickets` only): `or_(zendesk_status IS NULL, zendesk_status NOT IN _RESOLVED_ZENDESK_STATUSES)`. NULL (non-Zendesk / toy_dataset) rows still sweep exactly as before — the `IS NULL` branch is **load-bearing**, since SQL three-valued logic makes a bare `NULL NOT IN (…)` evaluate to NULL and would silently drop every non-Zendesk ticket. **New constant `_RESOLVED_ZENDESK_STATUSES`, deliberately NOT reusing `_SOLVED_BUCKET_STATUSES`** despite identical values: that one is a *display* decision (which statuses the queue's "Solved / Closed" row folds), this is a *pipeline gate*. Splitting "closed" back out as its own queue filter must never silently change what the sweep re-drafts.
- **Area 4 (follow-up reprocess) — confirmed correct as-is, NO code change.** The adapter's existing `ticket_status == "closed"` skip already produces the right behavior *by construction*: Zendesk auto-flips a `solved`/`pending`/`hold` ticket to **`open`** the moment a customer replies (documented platform behavior, confirmed with Jiacheng), so the adapter's live `ticket_status` read at process time already shows `open`, never `solved`. A genuine follow-up therefore reprocesses (correct), while an untouched resolved ticket is never reached at all. **Not yet empirically verified** against a real ConfMail-synced ticket's audit trail (`/api/v2/tickets/{id}/audits.json`) — flagged as an optional future check, **not a blocker**.
- **Tests (+8, tests-only piece A2-impl-2)** in `tests/test_reevaluation.py`, reusing the file's existing `_open_email`/`session`/`_StubRetriever` fixtures: `solved`/`closed` excluded · `new`/`open`/`pending`/`hold` still included · NULL still included (regression guard) · plus an end-to-end sweep test asserting the resolved pair keep their original `draft_text` while the active one re-drafts. **Mutation-verified**: deleting the guard fails 3; using a bare `not_in` without the `IS NULL` branch fails 14. Suite **359 passed / 7 skipped** (`-m "not ml"`; 351 baseline + 8).
- **Live verification on demo Postgres (A2-impl-3)**: 12 at-risk rows before (5 `closed`, 7 `solved`). `POST /api/v1/policies/reevaluate` returned **`open: 46`, not 58** — the guard is visible in the endpoint response itself. Sweep: `{open 46, redrafted 0, skipped_no_context 46}`. All 12 rows **byte-identical** after (`md5(draft_text)` + `updated_at` unmoved — not merely same content: no write occurred). Positive control needed a **synthetic probe row** (no existing ticket could prove the sweep still works — see below): with it, `open 46→47, redrafted 1` and the probe re-drafted to a real grounded reply; probe deleted, table restored to its exact 8-row breakdown.
- **⚠️ DEPLOYMENT NOTE — the guard requires a backend image REBUILD.** Caught during verification: the running image was 2h old, predated commit `a367c0c`, and `grep _RESOLVED_ZENDESK_STATUSES` in it returned nothing. Triggering the sweep then would have re-drafted the exact 7 solved tickets the fix protects. The backend has **no source volume mount** — `git pull` alone is NOT sufficient; `docker compose build backend` is mandatory, and the constant should be grepped in the image before trusting the fix is live.
- **Known separate issues — flagged, NOT fixed here**: (1) **the sweep is near-inert on this DB** — all 46 `toy_dataset` rows have `retrieval_context = NULL` (back-filled by mig `c1d2e3f4a5b6`) so they skip as legacy; combined with the guard, the sweep now has **no live work at all** here. Pre-guard, the only rows it could ever re-draft were the 7 solved Zendesk ones — i.e. its entire real workload *was* the tickets that shouldn't be swept. A future "the re-evaluate button does nothing" report traces to the NULL contexts, **not** to this guard. (2) 2 rows carry lowercase `'approved'` while `EmailStatus.APPROVED == "APPROVED"` — pre-existing casing drift, harmless for the sweep (not `DRAFT_GENERATED`) but it would break any exact-match filter on approved status. (3) `backend/models/rl_router_state.json` is **tracked** and mutated by every test run (RL bandit state persists) — it dirties the working tree on each `pytest` and will eventually be swept into an unrelated commit; consider gitignoring it or pointing the state path at a temp dir under test.
- **Not in scope / untouched**: area 3 (sync ingest `ZENDESK_SYNC_STATUSES` filter), area 4 (adapter follow-up), area 5 (manual `POST /emails/{id}/redraft` — still has no status guard; a chair re-drafting a resolved ticket is arguably intentional, so it may warrant a UI warning rather than a hard block — **separate piece, undecided**).

### 2026-07-21 (later) — Workstream B: Approve→Send chain (public/internal toggle + `target_status`) — Complete · live-verified on ticket 21567 · committed to `main` (`7e0727b`..`6d7e87a`)
Made the chair's "Approve & Send" actually reach Zendesk with a working visibility toggle and a status selector. Built B-impl-1a…6. No pipeline module touched.
- **The gap**: `/approve` and `/send` were separate endpoints and the frontend "Approve & Send" button called **only `/approve`** — `/send` was never reached from the UI (no `sendEmail()` wrapper existed). `SendRequest.public` worked, but `target_status` was collected in the UI and **silently dropped** (no backend field consumed it); the only status `/send` could set was a hardcoded `"solved"` on the public-reply path.
- **Backend fix**: added `target_status: Literal["open","pending","solved"] | None` to `SendRequest`, routed into `ZendeskSender`'s existing `set_status` param — `set_status = payload.target_status if not None else ("solved" if want_public else None)`. Backward-compat is **byte-identical when unset** (verified). Public/internal gating (`ALLOW_AUTO_SEND`+`public`) and tag logic (`ai_drafted`/`ai_auto_replied`, keyed on `want_public` only) unchanged. +6 tests (`test_zendesk_send_endpoint.py`, 359→365 non-ml).
- **Frontend fix**: added the missing `sendEmail()` wrapper + `useSendEmail` hook; new `SendVisibilityToggle` (Internal note ↔ Send to requester, **default OFF/internal** per Sahil); chained approve→send on the existing button (send fires from approve's `onSuccess`, `target_status` from the split-button selector, `public` from the toggle); **partial-failure handling** — approve-ok/send-fail shows a scoped retryable `ErrorBanner` ("approved locally… NOT sent"), approve **never rolled back**, Retry re-sends the same payload (never re-approves). +7 integration tests (real `QueuePage`+`EmailDetail`, API-layer spies; the repo's first react-query/full-page test), 13→20 vitest, tsc clean.
- **Live e2e (internal notes only) on ticket 21567 — both PASSED, independently confirmed via Zendesk read (not just app state)**: Test 1 (default, no `target_status`, toggle OFF) → new **internal** comment, ticket status **unchanged** (`new`); Test 2 (`target_status="pending"`, toggle OFF) → new **internal** comment + ticket status **`new`→`pending`** on real Zendesk. All writes `public=false`; **no public/customer-visible comment sent**; `ALLOW_AUTO_SEND` stayed `False`.
- **⚠️ Still-pending verification**: the **public/customer-visible send path was NOT tested** (needs `ALLOW_AUTO_SEND=True` + a safe ticket) — verify when safe.
- **Cosmetic gap (not a defect)**: local `Email.zendesk_status` doesn't refresh right after a write-back (shows stale value until the next sync) — reconciliation lag only.
- **⚠️ Deployment (recurring pattern)**: making `target_status` live required a **`docker compose build backend`** (no source volume mount; `git pull` alone insufficient) — same caveat as Workstream A. Rebuild also migrated demo Postgres `f7a1b2c3d4e5`→`af44a007c60f` (already-merged additive migs; 47 toy + 16 zendesk intact). **All Workstream B code is committed to `main`** (backend `7e0727b`/`a220f53`/`3f2a547`; frontend `0926179`/`45dcd38`/`f6dd724`/`818646f`/`6d7e87a`); only this CLAUDE.md entry is uncommitted.

### 2026-07-22 — Thread HTML rendering + queue-row tweaks + closed-ticket & requester-label fixes — Merged to main 2026-07-22 (from `feature/thread-html-queue-tweaks`, FF `f590673..b871daf`)
Conversation-view improvements + two backend correctness fixes. Reconciled with Sahil's just-landed send-path commits (`4126354`/`d1f5890`/`f590673`) via a clean auto-merge (different functions in `emails.py`). **No migration.** Gates: **372 non-ml passed / 7 skipped**, `tsc` clean.
- **Render Zendesk HTML in the thread**: `GET /emails/{id}/thread` now serves `html_body`, **server-sanitized with `bleach`** (new dep in `pyproject.toml`) — formatting allowlist; strips `<script>`/`<style>`+contents, event handlers, `style`/`class` attrs; `data:` permitted ONLY on `<img src>` for raster types (png/jpeg/gif/webp), blocking `data:svg` + `data:` on links (`_allow_attribute`). `ConversationThread` renders it via `dangerouslySetInnerHTML`; `.conf-html` CSS in `globals.css` restores lists/headings/etc. that Tailwind **preflight** flattens. Single/plain-body emails also render as HTML (escape→paragraphs+linkify, XSS-safe). Deliberate tradeoff: inline sender styles dropped → renders in our clean style.
- **Queue rows** (`EmailListItem`): show sender **name** + requester time; dropped the ticket-number and the **chair badge** (chair routing removed from list rows only — sidebar filter/detail untouched).
- **Requester-labeling fix**: the thread identifies the requester by **`author_id == ticket.requester_id`** (backend serves `is_requester`), NOT Zendesk `role` — chairs in this account often have role `end-user`, which mislabeled their replies as "Requester" (e.g. ticket 13780, assignee Ashley McCants). FE `turnMeta` uses `is_requester` with an `author_role` fallback for non-Zendesk.
- **Closed-ticket fix**: `_ingest_new_ticket` ingests a NEW **closed** ticket for visibility (status `ARCHIVED`) but **skips the pipeline** — no classify/draft (can't reply to a closed ticket). New `SyncResult.closed_ingested`; `solved`/others still drafted. Finding that prompted it: closed tickets WERE being drafted on ingest (default `ZENDESK_SYNC_STATUSES` includes `closed`, and `_ingest_new_ticket` classified regardless of status).
- **Demo DB** curated to **5 real tickets** (display-only; pulled read-only from live Zendesk): 2 multi-turn (15946, 13780), 1 image ticket (519 — 2 screenshots downloaded + embedded as base64 `data:` URIs), 1 rich single (21529 — list/table), 1 clean single (12307). Replaced the prior 14-email plain-text set.
- Runtime note: on this host the backend must start with thread caps (`OMP_NUM_THREADS=1` etc.) or it hits `libgomp: Thread creation failed` loading fusion; `.env` currently has `ZENDESK_POLLING_ENABLED=True` (run with a `False` override during inspection to avoid live-ingest).

### 2026-07-22 (later) — Light/dark theme toggle — Complete · frontend-only · manually verified both themes
Sun/Moon **pill switch in the Sidebar footer** flips the whole app light↔dark. **Defaults to dark → zero change for existing users** until they explicitly pick light. No backend/pipeline touched; no theme library added.
- **Mechanism (no `next-themes`)**: additive `:root[data-theme="light"]` CSS-var override block in `globals.css` applied to `<html>` — the bare dark `:root` block is **untouched** (purely additive). Wiring = a small `useTheme` hook (`{theme, toggleTheme}`, persists to `localStorage` key **`confmail-theme`**, defaults dark) + an **inline synchronous anti-flash `<script>` in `layout.tsx`'s `<head>`** that reads localStorage and sets `data-theme="light"` before hydration/first paint (try/catch → silent dark fallback; no flash on hard refresh). `ThemeToggle.tsx` = `role="switch"` pill (Moon left/dark ↔ Sun right/light), self-contained via the hook.
- **Why low-risk**: ~95% of the app themes via inline `style={{ …: "var(--token)" }}`, **not** Tailwind color utilities (the shadcn `hsl(var(--…))` color map in `tailwind.config.ts` is dead/unused), so overriding the token block recolors nearly everything in one place.
- **One real per-component exception — recharts**: SVG `fill`/`stroke` don't resolve CSS vars, so `analytics/page.tsx`'s chart palette became an explicit **theme-conditional `CHART_PALETTE`** object (grid/axis/surface/text/cursor + semantic hues), selected via `useTheme()` and threaded to child chart components as a `palette` prop. Dark values reproduce the originals 1:1.
- **Real bugs found+fixed during the light pass (not just polish)**:
  - **`CHAIR_PALETTE` (`lib/format.ts`) — genuine a11y failure**: pastel chair-badge **text** hues dropped to ~1.5–2.6:1 on white (badge `color` is used as text, not just a dot). Fixed with a separate darker (600/700-weight) light hue set; **`chairColor(chairId, theme)` now takes an explicit `theme` param (defaults `"dark"` for back-compat)**. (The task's hypothesis "text hues stay theme-invariant" did NOT hold — text was the primary failure.)
  - **`DiffView.tsx` add/remove highlights + analytics calibration-caveat callout**: inline `rgba` tuned for dark were near-invisible on light → theme-conditional variants **reusing T2 tokens** (`--success`/`--danger`/`--success-subtle`/`--danger-subtle`/`--warning-subtle`) for consistency with the app's semantic colors.
  - **Checked & left unchanged (avoid needless complexity)**: `zendesk-status.ts` status dots + `LiveStatusDot.tsx` — color only ever sets a small dot's `backgroundColor` and is **always paired with a readable text label**, so contrast is sufficient; theming a shared color module (also used by `SplitActionButton`) wasn't worth it.
- **Files touched**: `globals.css`, `hooks/useTheme.ts` (new), `app/layout.tsx`, `components/ui/ThemeToggle.tsx` (new), `components/layout/Sidebar.tsx`, `app/analytics/page.tsx`, `components/ui/DiffView.tsx`, `lib/format.ts`, `components/ui/ChairBadge.tsx` (gained `"use client"` — now uses a hook directly).
- **Verification**: `tsc --noEmit` clean + `next lint` clean on every changed file per piece; **manual pass confirmed correct rendering in BOTH themes** across queue, email detail (incl. diff view), dashboard, analytics (all charts), knowledge base, auto-replies, audit log — plus **no flash-of-wrong-theme on repeated hard refresh** in either mode. Built in pieces T1–T8/T10 (SDD-style). **Uncommitted** in the working tree (Sahil to commit).

### 2026-07-22 (later) — "Open in Zendesk" button in email detail header — Complete · manually verified · **uncommitted** (Sahil to commit)
Pill button in the email-detail header **badges row** that opens the matching ticket in Zendesk's agent UI in a **new tab** (`ExternalLink` icon + "Zendesk" label). **Self-suppressing** — renders **nothing** (not a disabled state) for emails with no Zendesk ticket. No pipeline module touched. Built in pieces Z2–Z4/Z6 (SDD-style).
- **Design decision — backend-serialized URL (Option B, chosen over a frontend env var)**: `_email_to_dict` (`api/v1/emails.py`) now emits **`zendesk_ticket_url`**, built from the existing **`ZENDESK_SUBDOMAIN`** config + `zendesk_ticket_id` → `https://{subdomain}.zendesk.com/agent/tickets/{id}` (agent-UI pattern **verified against a real live ticket** before coding; `?brand_id=` omitted as cosmetic/optional). **Null** when the subdomain isn't configured OR the row has no ticket id — **never raises**. **Why Option B**: keeps `ZENDESK_SUBDOMAIN` the single source of truth (backend-only), avoids a `NEXT_PUBLIC_*` frontend env var (which bakes into the browser bundle at build time → needs a rebuild to change, and would duplicate existing config). `config.py` **unchanged** — `ZENDESK_SUBDOMAIN` already existed; no new field.
- **`ZendeskLinkButton` (new, `components/email/`)**: fully self-contained — pass the raw (possibly null) `zendesk_ticket_url`, it renders the styled pill or nothing, so **no conditional wrapper needed at call sites**. Styled with CSS-var tokens (works in both themes; matches `Badge size="sm"` pill sizing, border + hover→`--accent` shift to read as an action not a status); `target="_blank" rel="noopener noreferrer"` + `aria-label="Open ticket in Zendesk (opens in new tab)"`.
- **Placement correction (worth preserving)**: originally scoped for the **title row (top-right)** from an early screenshot; **corrected mid-implementation to the badges row (row 3)** after a follow-up screenshot showed the actual intended position — a concrete example of confirming placement against a **visual reference**, not a verbal description.
- **Layout details worth preserving**: guaranteed **≥12px separation** from the last badge required a **wrapper `<div className="ml-auto flex items-center pl-3">`**, NOT padding on the button — because padding sits *inside* the button's own border and doesn't separate adjacent pills; only margin/padding **outside** the border creates real gap (worst case: row `gap-2` 8px + wrapper `pl-3` 12px = 20px). Wrapper guarded (`email.zendesk_ticket_url && …`) so no empty element renders for non-Zendesk rows. **Flex-wrap narrow-viewport** behavior (button drops to its own **right-aligned** line — anchored right in both states, consistent) evaluated and **left as-is** (option a); reasoned through flexbox semantics directly (CSS-deterministic, no JS-dependent behavior) rather than a live browser drag — `overflow-x-auto` rejected (scrollbar could hide the action), `shrink-0` unneeded (button has `whitespace-nowrap`).
- **Files touched**: `backend/app/api/v1/emails.py` (+`zendesk_ticket_url` in `_email_to_dict`, +`settings` import) · `backend/app/core/config.py` (**confirmed unchanged**) · `frontend/src/types/index.ts` (+field) · `frontend/src/components/email/ZendeskLinkButton.tsx` (**new**) · `frontend/src/components/email/EmailDetail.tsx` (import + wired wrapper).
- **Tests**: **+4 backend** (`test_email_serialization_redrafting.py`, Z2b) — URL-present (exact string), subdomain-not-configured→null, no-ticket-id→null, empty-string-subdomain→null. Full suite **376 passed / 7 skipped** (`-m "not ml"`; was 372). Frontend `tsc`/`next lint` clean per piece (no new vitest — presentational link).
- **Verification**: manual pass confirmed correct ticket-id match across multiple real synced tickets, null-suppression for non-Zendesk emails, correct rendering in both light/dark themes, and correct narrow-viewport wrap.

### 2026-07-22 (later) — Approve shortcut → Ctrl+Alt+S + collapsible shortcuts popover — Complete · live-verified · frontend-only
Two related review-queue changes. Approve moved off plain **`A`** to **`Ctrl+Alt+S`** (Marc's request); `E` edit / `C` reassign / `R` reroute unchanged. The always-visible hint row became a small **Keyboard-icon popover trigger** (`components/email/ShortcutsHintPopover.tsx`) — now shown at **all** widths (it was `hidden … sm:flex`, i.e. invisible below 640px *and* crowding the four action controls in the 640px+ narrow band; a fixed 32px trigger can't collide the way a multi-item text row did). Built S2a–S4.
- **⚠️ AltGr finding — a real, non-obvious bug class, not just a feature note.** On many non-US layouts (German, Polish, Spanish, Nordic…) **`AltGr` IS physically `Ctrl+Alt`**, and AltGr+letter types special characters. The first implementation deliberately **bypassed the "don't hijack typing" guard** for this combo (so a chair could approve right after editing the draft) — which would have made an AltGr-layout chair typing a character in the draft textarea **accidentally approve-and-send**. **Fix:** `Ctrl+Alt+S` now respects the *same* typing guard as the other shortcuts (never fires with focus in `INPUT`/`TEXTAREA`/`contentEditable`); the typing guard was hoisted to the top of the handler so it covers every shortcut. **Trade-off accepted**: the chair must click out of the draft before the shortcut works, in exchange for eliminating the accidental-send risk entirely.
- **Detection detail**: matched on **`e.code === "KeyS"`** (physical key) rather than `e.key`, so detection is layout-independent. The combo is still checked *ahead of* the blanket `if (metaKey||ctrlKey||altKey) return` guard (which would otherwise always reject it) — that guard bypass stays; only the typing-guard bypass was removed. `canApprove && !isApproving` are **inside** the combo condition on purpose, so a non-actionable press falls through **without** `preventDefault()` and never swallows a keystroke for nothing.
- **Escape hatch if the friction annoys**: a combo with no AltGr overlap (e.g. `Ctrl+Shift+S`) would allow safely re-bypassing the typing guard later.
- **Update (2026-07-24) — typing-guard bypass RESTORED for `Ctrl+Alt+S`, AltGr-gated (kept the combo, didn't switch to `Ctrl+Shift+S`).** The combo now fires **even while focus is in the draft textarea** again — the S2b2 "click out of the draft first" friction is removed — but with one added operand: **`!e.getModifierState("AltGraph")`**. So a genuine `Ctrl+Alt+S` approves, while a real **AltGr+S** (OS reports AltGraph active) types its character and never approves. The handler was reordered so the combo is checked **before** the typing guard; `E`/`R`/`C` still sit behind that guard, unchanged. **Trade-off:** swaps the prior *guaranteed-safe* behavior for a **heuristic** that trusts the browser/OS to report AltGraph correctly. Unit tests (`components/email/EmailDetail.test.tsx`) mock `getModifierState` and prove the handler **branches correctly** (approve when AltGraph false, no-op + no `preventDefault` when true) and that `E`/`R`/`C`'s typing guard is unchanged — **but jsdom cannot simulate real AltGr, so they do NOT prove hardware behavior**. ⚠️ **OUTSTANDING: manual verification on a real non-US / AltGr keyboard (German/Polish/Nordic…) has NOT yet been performed** — required before trusting this on those layouts; if AltGraph reporting proves unreliable there, fall back to the `Ctrl+Shift+S` escape hatch above (no AltGr overlap) or re-apply the always-on typing guard.
- **Architecture**: `Kbd` key-cap primitive **relocated** from a private `EmailDetail.tsx` helper → `components/ui/Kbd.tsx` (+ barrel export) for reuse. New dep **`@radix-ui/react-popover@1.1.21`** + themed `components/ui/popover.tsx` wrapper (same token-mapping convention as `dropdown-menu.tsx`) — a **true popover**, chosen over reusing `dropdown-menu.tsx` because the content is non-interactive prose, not a menu (no menu/menuitem semantics or roving focus).
- **Files**: `EmailDetail.tsx` · `components/ui/Kbd.tsx` (new) · `components/ui/popover.tsx` (new) · `components/email/ShortcutsHintPopover.tsx` (new) · `components/ui/index.ts` · `package.json`/`package-lock.json`.
- **Verification — live-tested, not just code-traced**: `Ctrl+Alt+S` approves with focus outside a text field · no-op with focus inside one · plain `A` does nothing · `E`/`C`/`R` unchanged · popover opens/closes correctly · both themes render correctly · trigger no longer collides with the action buttons at narrow widths. Gates: backend **380 passed / 7 skipped** (`-m "not ml"`), frontend **20 vitest**, `tsc` + `next lint` clean.

### 2026-07-22 (later) — Nav rail + collapsible filter column (three-column layout) — Complete · frontend-only (N2–N6)
Restructured the app chrome into three columns; no backend touched. Left→right: **icon rail** · **filter column** · **email list** · **email detail**.
- **Icon rail** (`Sidebar.tsx`): permanently icon-only at **52px** (`--rail-width` token in `globals.css`, single source — `<main>` offsets by the same var). Nav items are icons with hover/focus **tooltips** (`@radix-ui/react-tooltip@1.2.14` → themed `components/ui/tooltip.tsx`, one shared `TooltipProvider` per rail for skip-delay). Active state = filled `--accent-subtle` square (the old left-border indicator was dropped — at 36×36 it was mostly corner-radius + off-centred the icon 1px). Theme toggle moved into the footer as a `compact` icon variant. Branding text removed.
- **Filter column** (`queue/page.tsx`, **page-owned — NOT shell-owned**): holds `QueueFilterPanel`. **Collapsible** via a `PanelLeft` toggle (256px expanded ↔ 52px collapsed, `transition-[width] duration-200`, panel unmounted when collapsed); state persisted via `usePersistedState("confmail.filterColumnCollapsed")`.
- **Email list**: drag-resizable (`useResizableWidth`), **viewport-aware clamping** against a **dynamic `reservedWidth`** (rail + filter-col-current-width + handle) so collapsing the filters hands the freed ~204px to the detail pane; re-clamps on the expand direction (which consumes space). **Email detail**: `flex-1`, absorbs the remainder.
- **Removed portal architecture**: filters used to portal into a `SidebarSlot` inside the rail; that slot (context + provider + hook + `SidebarSlot.tsx`) is **deleted** — don't look for it or reintroduce it. `createPortal` is gone from the queue page; a filesystem regression test (`no-sidebar-slot.test.ts`) guards against its return.
- **Z-index inventory** (settled, don't re-investigate): rail **40** · mobile drawer/backdrop **30** · all Radix content (tooltip/popover/dropdown) + `PolicyDetailModal` **50** (portalled to body, above the rail) · the filter status/chair dropdowns are **native `<select>`** → browser top layer, outside the stacking context entirely, so no z-conflict is possible there.
- **New deps**: `@radix-ui/react-tooltip`, `@radix-ui/react-popover`. **Gates**: frontend **91 vitest / 9 files**, `tsc` + `next lint` clean, `next build` clean (all 11 routes). No screenshot tooling on this host — layout verified by deterministic CSS/width analysis + the test suite.

### 2026-07-22 (later) — Brand relocation: icon-only rail + desktop top bar (BR2–BR5) — Complete · frontend-only
Split the old rail brand block (icon + "ConfMail" + subtitle, sized for the dead 240px sidebar) so nothing overflows the 52px rail.
- **Rail brand → icon-only** (`Sidebar.tsx`): just the accent square + `Mail` icon, `h-9 w-9` (matches the nav-item footprint, aligns with the icons below); title/subtitle **removed** (not hidden). Non-interactive, as it always was.
- **New `DesktopTopBar`** (`components/layout/`): "ConfMail" · "Conference Email System" side-by-side (aria-hidden `·` separator), `hidden md:flex`, `fixed left-[var(--rail-width)] right-0 top-0`, height from a new **`--topbar-height` (56px)** token in `globals.css` (beside `--rail-width`). **z-30** — passed via a `className` seam from `AppShell` (BR3 built it in isolation, BR4 wired it). **Mutually exclusive** with the existing mobile top bar (`md:hidden` vs `hidden md:flex`, same 768px breakpoint — never both, never neither).
- **Corner geometry**: rail unchanged (`top:0`, full-height, `z-40`); the bar starts *after* the rail rather than over it, so z-30 sits safely below the Radix `z-50` overlays (no collision) and below the rail. `<main>` now offsets `pt-14` (mobile bar) · `md:pt-[var(--topbar-height)]` (desktop bar).
- **Latent bug fixed**: `queue/page.tsx` was a hard `h-screen` (100vh) that overflowed once `<main>` gained a top offset (already clipped 56px on mobile). Now `h-[calc(100vh-3.5rem)] md:h-[calc(100vh-var(--topbar-height))]`.
- **Filter-column toggle left-aligned** (BR3b): constant `px-2` (was `px-3`/`px-2` per state) → fixed 8px inset, no jump between collapsed/expanded, aligns with the rail's icon inset.
- **⚠️ Coupling risk**: `--topbar-height` · the mobile bar's `h-14` · `<main>`'s `pt-14` · the queue's hand-written `3.5rem` all independently represent **56px**. Regression-guarded (`layout-tokens.test.ts` asserts all four resolve equal + both tokens exist), but they remain four separate declarations if 56px ever changes.
- **Gates**: **106 vitest / 12 files**, `tsc` + `next lint` clean, `next build` clean (all 11 routes).

### 2026-07-23 — Optimistic queue advance + KB tweaks + LLM policy-conflict detection (2e) — Merged to main 2026-07-23 (FF `56c023f..4647780`, 5 commits)
One branch (`feature/optimistic-advance-and-kb-tweaks`), brainstormed → built → gated → live-validated.
- **Optimistic resolve+advance** (frontend): on submit the queue jumps to the next ticket right after the local `approve` (send gate), firing the Zendesk `send` in the BACKGROUND (no longer awaits it). A backgrounded send failure surfaces in a new non-blocking **queue-level notice** (`unseenFailedSends`, excludes the selected id so it never doubles the scoped banner) naming the `#ticket`, clickable to reopen; the ticket stays `SEND_FAILED` and visible. `/send` backend untouched.
- **KB tweaks 2a–2d**: similar-policy titles wrap (drop `truncate`); add-policy draft (title/content/category) **lifted to the KB page** so it survives the panel closing/reopening, cleared only on create; "Create internal policy" right-aligned (`ml-auto`); `/policies/similar` `top_k` 5→**10**.
- **2e — LLM policy-conflict detection**: `app/pipeline/policy_conflict.py::detect_conflicts` (one gated best-effort call mirroring the distiller — dispatch local/anthropic, no-op→None without a real LLM, strict-JSON parse, **drops hallucinated snippets** [not a verbatim substring] + unknown keys, never raises; model id from config). Candidates = top-10 similar ACTIVE policies (retriever indexes public+internal), excluding self. Triggers: `/similar` preview (not persisted); create (reuses the panel's precomputed report for the same text, else computes); edit/reactivate/revert-edit; `POST /policies/{key}/recheck`. Persisted in **`PolicyDocument.conflict_report` JSON** (mig **`d5e6f7a8b9c0`**, additive, down_rev af44a007c60f; applied to demo SQLite). Best-effort `_refresh_conflict_report` never breaks a KB write; list `_prune_stale_conflicts` drops conflicts vs now-retired/superseded targets (`active_keys`). **Display is inline, never a modal**: Add-panel Conflict/No-conflict badges + one-line summary + highlighted conflicting phrase (`HighlightText`); KB-card expandable "⚠ N conflicts" strip (explanation + quoted snippet + "checked Xm ago" + per-card **Re-check** + **inline Edit** of each conflicting policy via `ConflictEditRow`→`usePolicy`+`PolicyEditor`); dismissible page banner after create/reactivate linking to the new **"Conflicts only" filter** (`GET /policies?has_conflicts=true`, filters after the prune, forces status=active in the UI).
- **Gates**: backend **416 passed / 7 skipped** (`-m "not ml"`; +22: detector 13, endpoint 9); frontend `tsc`+`eslint` clean, **vitest 117** (+9: HighlightText 5, PolicyList strip 4). **Live-validated** on the real model (gpt-5.5): a "reviews within 5 days, no exceptions" draft correctly flagged a conflict with an exact snippet; the conflicts-only filter live-returned exactly the policies with live conflicts. Spec (gitignored): `docs/superpowers/specs/2026-07-23-policy-conflict-detection-design.md`. **Deferred**: a full re-check-everything sweep on any KB change (cross-policy staleness beyond retire/supersede handled by timestamp + manual re-check).

### 2026-07-23 (later) — Highlight/selection UX polish (conflict edit + AI-draft editor) — Pushed to main (`3dd7796..e68a90d`)
Follow-up review-iteration on the 2e highlight surfaces; frontend-only.
- **In-box conflict highlight**: editing a conflicting policy from the strip now highlights the conflicting passage INSIDE the content box (new `components/kb/HighlightTextarea.tsx` — a mirrored backdrop paints the marks behind a transparent-background textarea that holds the VISIBLE text, so caret/selection stay aligned; **auto-sizes to content so it never scrolls**, which is what keeps the marks on the right characters — a scrollbar narrows the textarea's wrap vs. the backdrop and drifts them). Replaced the earlier read-only reference box.
- **Editing preserves visibility**: `PolicyEditor` no longer defaults the visibility control to internal when the current value is unknown — it HIDES the control and OMITS the field so the backend keeps `base.visibility` (public stays public). Shown/editable only when the current visibility is known.
- **Brighter marks**: `rgba(239,68,68,0.35)` in `HighlightText` + `HighlightTextarea` (was the too-faint `--danger-subtle`), readable in both themes.
- **AI-draft `[CHAIR: …]` editor fix** (`EmailDetail.tsx` `HighlightedDraftEditor`): it had the fragile orientation (visible text on the backdrop, transparent text on the textarea) → selection drifted off the text once a draft scrolled. **Flipped to the robust orientation** (visible text on the textarea, only the placeholder glow-boxes on the backdrop) + auto-size ⇒ selection is pixel-accurate. Dropped the placeholder's `fontWeight:600` (bolding only the box changes glyph advances → misalign). Added **click-to-select**: a plain click inside a `[CHAIR: …]` token selects the whole token (never hijacks a drag-select).
- **Gates**: `tsc`+`eslint` clean, frontend **vitest 120** (+3: `PolicyEditor.test` visibility ×2, conflict-strip in-box highlight ×1). Backend untouched. Commits `94ec8be` (kb) + `e68a90d` (email).

### 2026-07-26 — NEW category `backend/scripts/recovery/` + read-only internal-note discovery script — Built, NOT yet run against real Zendesk (Sahil to review + run) · uncommitted
Incident: a chair's Zendesk composer was left toggled to "internal note", so ~2–3 days of replies were posted `public: false` — written to the ticket, invisible to the requester. This piece is **discovery only**; the republish/write-fix is deliberately NOT built (gated on Sahil reviewing the report with Marc).
- **`backend/scripts/recovery/` is a new script category** (README inside): one-off incident-recovery / data-fix scripts, kept separate from `backend/scripts/` (regular pipeline tooling — seed/eval/ablations). House rules encoded in the README: never imported by `app/`; reuse real modules for auth/config instead of re-implementing; diagnostics are read-only and write-capable repairs are separate, obviously-named files; output is gitignored (quotes real ticket PII); every one-off still gets a test, because these run against production data once, under time pressure, with no staging rehearsal.
- **`find_internal_notes.py`** — standalone (not part of FastAPI), reuses `app.integrations.zendesk.credential_provider.get_zendesk_credential_provider` for auth (**zero duplicated OAuth logic**) and `settings` for the subdomain. Touches no ConfMail DB / classifier / retriever / drafter.
- **Read-only by construction, not by convention**: module constant `READ_ONLY` + an `assert` in `main()`, and the httpx data client is built with an `event_hooks={"request": [_reject_non_get]}` guard that raises `WriteAttemptError` on any non-GET **before it is sent** — so an accidental write cannot leave the process even if a future edit adds one. (The OAuth provider's token POST uses its own client, untouched — deliberately not routed through the guarded one.)
- **Two-phase whoami checkpoint (the safety gate, non-bypassable)**: a bare run calls only `GET /users/me.json`, prints id/name/email/role, and **stops having searched nothing**. Proceeding requires `--confirm-author-id <id>` **matching** the API-resolved id (mismatch → exit 3, no search). Chosen over an interactive `y/n` prompt on purpose: it forces an affirmative act on a value that had to be read off the phase-1 output, instead of a prompt that gets reflexively dismissed — and it works non-interactively.
- **Enumeration = incremental cursor export** (`/incremental/tickets/cursor.json?start_time=`), per `docs/ZENDESK_API.md` §2 and matching the production poller. **Search API rejected**: its results lag indexing (a ticket updated minutes ago can be missing — fatal when the goal is *every* affected comment) and it hard-caps at 1,000. Export has neither problem and includes archived/closed. Rate limits honored: 6.5s between export pages (10 req/min cap), 0.15s between per-ticket comment fetches, `Retry-After` on 429, 5xx backoff. `status == "deleted"` tickets dropped (never fetched). `--max-pages` runaway guard **prints a loud INCOMPLETE warning** rather than silently passing for a full sweep.
- **Filter** (all three must hold): `author_id == me.id` · `public is False` · comment `created_at` inside the window. `public` is matched **strictly against `False`** — an absent/non-boolean flag is *not* reported, because these findings feed a republish decision and anything we can't positively confirm was private must not be queued for resending. Window `--days` (default **4**, margin beyond the ~2-day estimate), boundaries inclusive.
- **Two report formats** in gitignored `backend/scripts/recovery/output/`: `internal_notes_<stamp>.md` (grouped by ticket, agent-UI link, timestamps, 200-char plain-text preview) + `internal_notes_<stamp>.json` (`schema_version: 1`, identity, window, stats, `affected_ticket_ids`, per-finding rows) so the future republish script consumes it **without re-scraping**. Ticket links use `agent_ticket_url()` derived from the provider's `base_url` — subdomain stays single-sourced in config, **not** hardcoded to `aaai`.
- **Tests — `backend/tests/recovery/test_find_internal_notes.py`, 44 new, all pass** (new `tests/recovery/` package — `__init__.py` present, matching `tests/` itself being a package; flattened from an initial `tests/scripts/recovery/` since nothing else in the suite nests two deep). Fully mocked Zendesk (`FakeZendeskClient` records every path, so assertions are on *which endpoints were reached*): filtering (public/author/window/boundaries/non-boolean flag/mixed batch) · both report formats incl. an exact-dict assertion on the finding shape (the republish contract) · **the checkpoint halts before any search** — asserted as `client.paths == [ME_PATH]` plus "no files written", not merely on the exit code · read-only guard (all four write verbs rejected, real client's `.post` raises, plus a source-scan test that no write verb appears) · pagination/cursor, epoch `start_time`, deleted-ticket skip.
- **Gates**: new tests **44 passed**. Full non-ml suite **521 passed / 20 failed / 7 skipped** — the 20 are **pre-existing env drift, not regressions**: verified identical (20 failed / 477 passed) with the new tests excluded. Cause: dev SQLite is at `af44a007c60f`, missing migration `d5e6f7a8b9c0`, so `policy_documents.conflict_report` doesn't exist (`cd backend && alembic upgrade head` fixes it — deliberately not run against Sahil's local DB).
- **Next**: Sahil reviews the code, runs phase 1 to confirm the identity is Marc's, then phase 2. Report reviewed with Marc **before** any republish script is written.

### 2026-07-26 — Policy Citations panel: manual invoke made visible, then restored to compact rows — Complete · frontend-only · committed to `main`
Chairs reported the citation panel had suddenly become huge. **Nothing was restyled** — the cause is worth remembering because it will recur: the panel picks a render branch by *data availability*, and a backend change woke a dormant branch.
- **Root cause (`0c6f946`, citations 2a)**: `_email_to_dict` began serializing `retrieved_chunks` for the first time. The tall-card markup had existed since **`bc69bb35` (2026-06-27)** but was **unreachable** — `email.retrieved_chunks` was always undefined, so every ticket fell through to branch 2 (compact ID pills). Populating the field flipped every ticket to branch 1 overnight. A `git log -S"line-clamp-3"` over the window returns **nothing**: no layout commit exists. **Lesson: when a panel's appearance changes with no CSS diff, check whether its data source started being populated.** (An earlier investigation reported "CitationCard unchanged for weeks" — true of the source, wrong as an answer, because it never asked which branch was live.)
- **`chunks.slice(0, 3)` was hiding manual invoke** (`d648d0d`): the cap predated the feature, and since `MAX_RETRIEVED_CHUNKS=3` the chair-forced policy is appended as the **4th** chunk — always sliced off. Chairs got a green "policy added" confirmation and saw no change. Cap removed; the list is bounded server-side (`top_k` at both producers + at most **one** scalar `forced_policy_key`, deduped), so `retrieved_ids ≤ MAX_RETRIEVED_CHUNKS + 1`. **Do not reintroduce a client-side cap.**
- **Final design** (`a69c3d7`, `9b2c32c`): one line per policy — icon + `title · category` on a single truncating line, category as **muted inline text, never a badge** (a badge adds height, which was the complaint). No inline body preview; the whole row is a button opening `PolicyDetailModal` for the full text (hence no inline expander — nesting a button in a button is invalid). Kept: the **Add Policy** trigger, the chair-forced **"Added by you"** marker (`Badge variant="accent"`, new variant; driven by `retrieval_context.forced_policy_key`, which is durable and **not** session-gated like the 5d outcome banner), and click-to-modal. Titles kept over raw `policy_186` ids — that data only arrived with 2a.
- **Category is nullable and the fallback is load-bearing**: `policy_documents.category` is `String(64) NULL`, and the KB Add-Policy form posts `category.trim() || null` for an empty box (title/content are `required`, category is not). So a chair-added policy legitimately may have none → the whole `· category` segment is omitted rather than rendering `· null`. Built defensively **because the write path permits it**, not because one was observed: the local corpus has **0 nulls across 94 rows**, but it holds only **one** chair-added policy and **none of Marc's** (`int_paper-deletion__v2` etc. live on the chairs' own deployment, not this DB). Worth running `SELECT policy_key, title, category FROM policy_documents WHERE category IS NULL OR btrim(category)=''` on that deployment.
- **Also fixed en route**: `retrieval_context` was serialized by the API but **never declared on the frontend `Email` type** — added as `RetrievalContext`.
- **Gates**: frontend **216 passed / 29 files**, `tsc` + `next lint` + `next build` clean after **each** commit. Commits `a69c3d7` (compact rows) → `9b2c32c` (inline category) → `0ebf2ab` (tests) → this entry.

### 2026-07-26 — Policy EXCLUSION: chairs can remove a citation and re-draft without it — Complete · 9 pieces · committed to `main`
The counterpart to manual invoke: a chair can now remove ANY citation (retriever-found or their own forced pick) and re-draft on what remains. Built as `excluded_policy_ids: list[str] | None` on `RedraftRequest` — **no migration**, persisted in the existing `retrieval_context` JSON. Multi-forced "add several" was deliberately **not** built (drafter-prompt risk); the chair swaps by removing what they don't want and forcing the one they do.
- **Exclusion is a PRE-FILTER, and that is why it carries none of the multi-add prompt risk.** Excluded chunks are dropped from the ranked set before the drafter is called, so they never enter the prompt — there is nothing to instruct the model about. `drafter.py` is **untouched** and no real-LLM re-validation was needed, unlike the forced-policy wording which took two rounds (task 4 → 4d) to stop the model abandoning the requester's question.
- **⚠️ Matching is on LINEAGE ROOT, never the literal `policy_key`** (`resolve_lineage_roots` in `orchestrator.py`, shared with the sweep). `edit_policy` mints a NEW key for an edited policy (`int_foo` → `int_foo__v2`) and retires the old row, so an exact-key exclusion goes stale **on precisely the event that runs the sweep** — a KB edit — and the chunk the chair removed would quietly reappear under its new name. Note the asymmetry with forcing: **forced fails safe** (a retired key resolves to `None`, so it just stops forcing) while **exclusion fails open** (an unmatched key stops excluding). That asymmetry is the whole reason root matching is mandatory here and was optional there. `RetrievedChunk` carries no `root_key`, so roots come from one batched `get_by_keys` → `PolicyRepository._root_of(row)`, with an unknown key mapping to **itself** so a bad id never widens the filter.
- **Precedence: EXCLUSION ALWAYS WINS over forcing.** If a key is both forced and excluded, no forced chunk is appended (`forced_excluded` guard, surfaced on the trace). Without it the ordering silently *undoes* the removal — the key is filtered out of the ranked set, then resolved and appended straight back. That would have been a contradiction resolved by accident of sequencing rather than by rule. Removal is the more recent, more specific intent, and failing toward fewer policies is the safer direction.
- **Minimum-1 guard lives in the ENDPOINT, not the pipeline** — because the re-draft is async (202 + background task), a check after scheduling has no response left to reject with and could only be a silent no-op or a stranded ticket. It compares against the email's stored `retrieval_context.retrieved_ids` (the exact cards the chair is looking at) rather than dry-running retrieval, which would re-issue a distiller model call and, being non-deterministic, could disagree with the run that follows. **409** with a plain-English reason. A still-standing forced policy **rescues** an otherwise-empty set — "remove all three and ground on this one instead" is the swap workflow the feature exists for and must not be rejected. Fails OPEN for a legacy NULL-context row. Best-effort by nature: the redraft re-runs retrieval, so the guard covers the common case, not a hard invariant.
- **Sweep parity**: `_fresh_topk_ids` re-applies exclusions in the SAME order as `_compute` (filter → force, exclusion beating a conflicting forced key) and carries `excluded_policy_ids` into `new_ctx`, so a chair's curation survives someone else's KB edit. Both paths share `resolve_lineage_roots` / `resolve_forced_chunk` so the rules cannot drift — the pattern established in 3e. A normal ticket's context still keeps exactly its five keys (regression-pinned).
- **Reset semantics**: exclusions are per-draft. `reprocess_email_with_thread` (follow-up) calls `_compute` with no extras and never reads prior context, so a follow-up resets to normal retrieval. Only the KB-edit sweep carries them forward, deliberately.
- **Frontend — removal is STAGED, not per-click** (`EmailDetail.tsx`): the X marks a row, and one **"Re-draft without them"** submits the whole set. Per-click would be 3× model spend for two throwaway drafts, and worse: each re-draft is async and `_compute` recomputes from the row's subject/body knowing only *its own* exclusion list, so overlapping clicks would let the last writer silently drop the earlier removals. Also matches the Add-Policy dialog (select stages, Approve commits). The remove control is a **SIBLING** of the row button, never nested (the row is itself a `<button>`); hover-revealed, always visible once staged. Removing your own forced pick clears the "Added by you" badge and the success banner (restored on un-stage, since the durable flag is untouched). The minimum-1 rule is enforced **at the control** — the last remaining row's X is disabled with the reason in `title` + `aria-label` — plus a submit-level backstop.
- **Gates**: backend **20 failed / 551 passed / 7 skipped** with the failure set **byte-identical to the pre-session baseline** at every step (the 20 are the known stale dev-SQLite `conflict_report` drift); frontend **229 passed / 29 files**, `tsc` + `next lint` + `next build` clean. Mutation-verified at each risky step: dropping the forced/excluded guard, swapping root matching for exact-key, and dropping the sweep carry-forward each fail their specific test. Commits `761477d` → `c04134a` → `781a5a2` → `d904685` → `cfe214f` → `9c2ab03` → `2b3ecbf` → `2791576` → this entry.

### 2026-07-28 — "Active but superseded" stuck policy: `reactivate()` clears the stale pointer + UI surfaces the state — fix commit `1a3cc8a`
Chair-reported incident (policy_174, AAAI, 2026-07-27): a policy showing a green **active** badge rejected every edit with `Only an active, current policy can be edited.`
- **Root cause — the two lineage columns are written together but were not cleared together.** The edit guard ([`policies.py`](backend/app/api/v1/policies.py) `edit_policy`) requires `status == "active"` **AND** `superseded_by IS NULL` — "active" and "current" are **independent scalar columns**, not `resolve_lineage_roots` and not a `max(version)` comparison. `edit_policy` sets both (`status="inactive"` + `superseded_by=<new key>`), but `reactivate()` only flipped `status` back. Reachable sequence: **edit A → retire A__v2 → reactivate A** ⇒ A is `active` with a stale forward pointer, uneditable **forever**. Confirmed in production: exactly 1 row, isolated not systemic, but fully reproducible on any policy.
- **Fix**: `reactivate()` now also clears `superseded_by`. **`supersedes` is deliberately KEPT** — the two pointers are different kinds of fact: `superseded_by` is a *live claim* ("a newer version replaced me") that reactivation genuinely falsifies, while `supersedes` is an *immutable historical record* of how the row was created, and is what `revert-edit` walks backward ([`policies.py`](backend/app/api/v1/policies.py) gates on `not tip.supersedes`). Clearing it would silently orphan a reactivated row from its lineage. It is also the only backward link — `superseded_by` is reconstructible from it, not vice versa. **This mirrors `revert_edit`, which already cleared exactly this one pointer** — the fix makes `reactivate` consistent with the existing correct path rather than inventing a rule.
- **`retire()` correct as-is, unchanged**: a standalone soft-delete implies no successor, so writing a `superseded_by` would fabricate one. The asymmetry with `edit_policy` is intentional. Fixing the single write in `reactivate` closes the state entirely. Cannot create two editable tips — the reactivate endpoint already 409s if any lineage sibling is `active`.
- **UI (`PolicyList.tsx`, the only surface with a status badge + Edit action)**: `status=="active" && superseded_by != null` now renders a **`superseded`** badge (`warning` variant) **in place of** the misleading green `active` pill, with **Edit disabled** + `title` tooltip + a visible inline note naming the successor's title (via the existing `resolvePolicy` prop; falls back to its key when filtered out of view), linked by `aria-describedby` so a screen reader gets the reason not a bare dimmed control. Retire stays enabled. ⚠️ **`isSuperseded` is gated on `isActive` on purpose**: an **inactive** row carrying `superseded_by` is the NORMAL post-edit state of every edited base, so an ungated check would mislabel routine history *and* hide Reactivate behind a disabled Edit — regression-pinned.
- **Tests +11, all mutation-verified** (removing the fix / forcing `isSuperseded=false` fails exactly the detection tests, never the invariant ones): backend +4 in `test_policy_versioning.py` (bug sequence end-to-end incl. the edit now succeeding; `supersedes` backlink survives a 3-deep lineage, proven by a subsequent revert-edit; never-edited reactivate unchanged; `retire` writes status only) · frontend +7 in `PolicyList.superseded.test.tsx`. Gates: backend **560 passed / 7 skipped** (the **20 failures are the known pre-existing dev-SQLite drift** — missing mig `d5e6f7a8b9c0` ⇒ no `policy_documents.conflict_report`; `alembic upgrade head` clears them, deliberately not run on the local DB); frontend **264 passed / 36 files**, `tsc` + `next lint` clean.
- **Production remediation already DONE, separately from this code work**: Ida confirmed the authoritative content version, `superseded_by` was cleared by a direct UPDATE, and she has verified she can edit policy_174 again. This code change touched no data — it prevents the state from recurring.
- **Flagged, NOT fixed**: create-with-`retire_keys` logs `after={"status": "inactive", "superseded_by": row.policy_key}` ([`policies.py`](backend/app/api/v1/policies.py) `create_policy`), but `retire()` only ever writes `status` — the column is never set, so the audit trail overstates what happened to the row.

### 2026-07-28 (later) — Queue pagination: `total` transient-zero window — **TWO** confirmed defects fixed (frontend-only)
One root cause, two independent user-visible bugs. `useEmailQueue` returned `total: data?.total ?? 0`, and with no `placeholderData` React Query has **no data at all for a key it has not fetched yet** — so `total` read **0** for one render on every key change. `EmailWorkspace` derives `pageCount = Math.max(1, Math.ceil(total / QUEUE_PAGE_SIZE))`, which turns a 0 total into **"1 page"**, and the stale-page clamp (`page > pageCount → setPage(pageCount)`) then reset the page. **`Math.max(1, …)` manufacturing a floor of 1 out of "unknown" is the whole bug** — the clamp could not distinguish *"the set shrank to 1 page"* from *"we don't know the count yet"*.
- **Defect 1 (reported): clicking a page number silently bounced back to page 1 on the FIRST click, worked on the second.** Nothing was stale and no closure was involved — the discriminator is purely **whether that page's query key has cached data**. First click → uncached → `total` 0 → clamp → page 1; the in-flight fetch still completes and populates the cache, so the second click reads the cached `total` synchronously and sticks. Correlated with "pages just exposed by truncation" only because those are exactly the pages never visited. Reproduced identically via the **Next chevron**.
- **Defect 2 (NOT reported, found during verification — a separate PRE-EXISTING bug, not a side-effect of fixing defect 1): the persisted queue page was silently discarded on every hard reload.** On a cold cache there is no previous data for `placeholderData` to keep, so `total` is 0 on the first render and the clamp reset the page before the count ever arrived. Proven empirically, not argued: with `confmail.queuePage=5` the workspace requested offsets **`[400, 0]`** and landed on page 1. It only ever worked inside an SPA session because the cache was warm. **This is fixed by the same `hasCount` guard and deserves its own line — it predates this session's work and was never part of the original report.**
- **Fix 1 — at the source** (`useEmailQueue.ts`): `placeholderData: keepPreviousData` (**v5 API**: import the `keepPreviousData` helper; v4's boolean `keepPreviousData: true` is REMOVED in v5 — verified against installed **5.101.1**, not inferred from the semver range). Fixing the hook rather than only the clamp keeps `total` honest for every consumer (count text, empty states). No conflict with the `staleTime: 30_000` / `retry: 1` defaults — both are orthogonal to "what is returned for a key with no data". Blast radius is one caller: dashboard (`["emailQueue"]`) and auto-replies (fixed `{lane:"faq"}`) never change key. `isFetching` deliberately NOT exposed — grep confirmed zero consumers.
- **Fix 2 — the clamp keeps its job, gated** (`EmailWorkspace.tsx`): `const hasCount = total > 0;` … `if (hasCount && page > pageCount)`. **Gated on `total > 0`, NOT on `!isLoading`** — with placeholder data present v5 reports `status:"success"`, so `isLoading` is false mid-transition AND false in the error state; it no longer identifies "count unknown". `total > 0` states the actual precondition. A genuinely empty set skips the clamp harmlessly (`Pagination` renders nothing at `pageCount <= 1`). The clamp was **kept, not deleted** — it still has a real job (the set shrinking under you on the 15s poll would otherwise strand the user on an empty page).
- **`placeholderData` does NOT cover the error window** — it applies while the query is `pending`; on `error` v5 sets `data: undefined`, so `?? 0` returns. Confirmed empirically (offsets `[0,100,0]` after a rejected page-2 fetch). The `hasCount` guard is what closes it, so a failed fetch now shows the error **without** also yanking the chair back to page 1.
- **Deliberate approved UX change**: with placeholder data the list shows the PREVIOUS page's rows for ~200ms instead of a loading state (standard keep-previous pagination — no layout collapse between pages).
- **Tests +10, both fixes mutation-verified INDEPENDENTLY** (disjoint failure sets prove neither is load-bearing for the other): dropping `placeholderData` fails 3 (hook transient · page-number click · Next chevron); dropping the `hasCount` guard fails 3 (cold mount · failed fetch · **and the genuine-shrink test — without the guard the true clamp lands on page 1 instead of the correct last page 2**). New `useEmailQueue.transient.test.tsx` (3) + `queue-pagination-clamp.test.tsx` (7) mock **only the network layer** so `total`'s pending/success/error transitions come from React Query itself.
- ⚠️ **Why the original bug slipped through, and the standing trap**: all 4 existing pagination test files `vi.mock` `useEmailQueue` to return a **constant `total` regardless of params**, so the transient window cannot exist in them. Deliberately NOT rewritten — a mock faithful to the pre-fix hook breaks those tests, and one faithful to the post-fix hook *is* `keepPreviousData` and so can never catch its own regression. Instead `queue-pagination-behavior.test.tsx` + `queue-pagination-fetch.test.tsx` gained **SCOPE LIMIT headers** naming the blind spot and pointing at the real-hook file. **Anything timing-dependent (loading windows, the clamp, cold mounts, fetch errors) belongs in `queue-pagination-clamp.test.tsx`, never in a hook-mocked file.**
- **Gates**: frontend **274 passed / 38 files** (from 264/36), `tsc` + `next lint` clean. Backend untouched.

### 2026-07-29 — Drafter AoE current-time injection + top-k default 5 · merged, deployed to AAAI prod, timing backlog redrafted
Two backend changes shipped together (`feature/drafter-aoe-time`, `config/default-topk-5`), merged FF to origin/main (`d240962..6c17204`), both branches deleted.
- **AoE time injection (drafter only)** — commit `cb76364`. The model has no clock, and a hosted chat-completions API supplies at most a coarse UTC *date* (no time-of-day, and the wrong frame for AoE deadlines — verified empirically against the live endpoint). So the drafter now states "now" explicitly in **Anywhere-on-Earth (UTC−12)** in BOTH the system and user prompts and tells the model to treat it as the authoritative current time — deliberately with **no** usage/deadline instructions. `_now_aoe_label()` (wall-clock UTC → AoE label, computed once per draft) threads into `_system_prompt(now_aoe)` + `_build_user_prompt(..., now_aoe=)`; both default `None` → byte-for-byte the pre-clock prompt, so every baseline test stays green. Template/fallback providers untouched (no model call). New `test_drafter_aoe_time.py` (UTC→AoE conversion incl. day-boundary rollback · both injection points · one label into both messages via `draft()`); style-guide + forced-policy baselines relaxed for the leading clock line.
- **top-k default 5** — commit `6c17204`. `MAX_RETRIEVED_CHUNKS` 3 → 5 (`config.py` + `backend/.env.example`); reverts Sahil's `bda86eb`. Per E003 `hit@3 == hit@5`, so this is a grounding-breadth choice, not a hit-rate one. Two forced-policy tests re-pinned to `settings.MAX_RETRIEVED_CHUNKS` (was hardcoded `3`) so the default can move without re-breaking them; the forced-policy EXTRA-slot invariant is unchanged. Combined gate green across drafter/forced-policy/retrieval/reeval/redraft/send-gate subsets.
- **Deployed to AAAI prod** (`~/confmail-deploy/conference-email-system`, Docker Compose `-p confmail`, `-f docker-compose.yml -f docker-compose.prod.yml`): **backend-only** rebuild (`up -d --build backend`) — no migrations, no frontend change → frontend/db never restarted, loopback binding preserved. Prod `backend/.env` set `MAX_RETRIEVED_CHUNKS=5` (its own value overrides the config default). In-container check confirmed effective top-k=5 + AoE present in both prompts. Access via a two-hop SSH tunnel (laptop → Delta `dt-login01` → AAAI `51.222.153.170`, ports 8000/3000).
- **Timing backlog redrafted** — of 764 active `draft_generated` tickets (new/open/pending; solved/closed excluded), redrafted the **179 "draft-side" set** where AoE plausibly changes the answer (current draft mentions a deadline/date OR carries a timing `[CHAIR:]` placeholder) — ~73% narrower than a blanket redraft; the 86 inquiry-only "noise" tickets (incidental "when will I hear"/session-chair threads) were deliberately skipped. ⚠️ `/redraft` runs the **FULL pipeline** (`reprocess_email`: re-classify via distiller → retrieve@5 → route → draft-AoE) = 2 model calls/ticket and **overwrites the draft with NO `draft.history[]` backup**. Chosen over the leaner draft-only path (the `reevaluation` sweep reuses stored query+intent → 1 call, no classification drift) purely for speed. Mitigations: snapshot of all 179 prior drafts to `~/redraft_backup_20260729_0940.tsv` first (restore path); paced detached loop, concurrency-capped at 3 (keep model calls in flight low so timeouts don't overwrite good drafts with "Draft unavailable" fallbacks); loop reads ids from **FD 3** so `docker exec`/`curl` can't swallow the id list; foreground `sleep` is blocked on the Delta login node, so the loop was written as a file and launched detached (`nohup`). Result **179/179 refreshed, 0 stuck, 0 fallback** (2 initial timeout-fallbacks retried individually to success). AoE confirmed live in output (e.g. *"the paper submission deadline has not yet passed … until July 28 UTC-12"*; a past July-21 deadline treated as closed).

### 2026-07-29 (later) — KB policy previews: respect embedded newlines (1a–1d) — Complete · frontend-only · 6 commits on main
Three KB surfaces rendered policy bodies as one run-on paragraph. **Pure CSS gap — the data was clean at every layer** (traced DB `Text` → `_policy_dict` verbatim passthrough → axios JSON → JS string; no `strip`/`splitlines`/normalisation anywhere), so nothing backend or schema-side was touched. Root cause: the body elements set no `white-space` and nothing in the inheritance chain does either (`globals.css`'s only such rule is `.conf-html pre`, and no typography plugin is enabled), so they fell to the CSS initial value `normal`, which collapses newline runs into spaces.
- **Why `pre-wrap`, not markdown or `pre-line`**: the corpus separates list items with a **single `\n`, not a blank line** — `int_program-commitee-roles-and-structure` (the reported policy) has **8 newlines and 0 blank-line breaks**; 89 of 157 live policies contain `\n`. So a markdown/paragraph-based renderer would not have fixed it at all. `pre-line` was rejected because it collapses leading whitespace and would flatten the nested 4-space indents real chunks use (e.g. `policy_102`'s `    - Phase 1:`). In-repo precedents already shipping the right thing: `PolicyHistory.tsx:270` (`whitespace-pre-wrap` **with** `line-clamp-2` — proof the combination works here), `PolicyDetailModal`, and this workstream's own sibling, the `PolicySelectPopover` **detail view**.
- **Three sites fixed**, each an inline `whiteSpace: "pre-wrap"` added to an existing `style` object; **`className` byte-identical at all three — no truncation/clamp behaviour changed anywhere**: (1) `PolicyList.tsx` card body, collapsed+expanded toggle — `499ee3c` / tests `a1756a7`; (2) `AddPolicyPanel.tsx` similar-policy preview — `77b6ae3` / tests `6fc1699`; (3) `PolicySelectPopover.tsx` **search-result** preview — `bebca9c` / tests `58ec552`.
- **Site 3 was NOT in the original 3-site suspicion list** — found during the discovery pass by grepping every `.content` render, and it is a **different element from the same file's already-correct detail view**. It also differs from the other two: its `line-clamp-2` is a **static string, no `cn()`, no expand/collapse toggle**, so there is no second state to guard (the test asserts the toggle's *absence* so a future "add Show more here too" must bring its own expanded-state guard).
- **`HighlightText` compatibility verified against emitted markup, not assumed** (site 2 renders through it): compiled the real component and server-rendered all three branches — no snippets, snippet mid-line, and **snippet spanning a newline**. Every emitted element is inline (`<span>`/`<mark>`; no `<br>`/`<p>`/`<div>`) and **no child declares its own `white-space`**, so the parent's `pre-wrap` inherits — which is what makes the spanning case work, where the newline sits *inside* the `<mark>`. Newline count exactly preserved in all three. `HighlightText` needed no change.
- **Radix Dialog compatibility verified from compiled CSS** (site 3): Tailwind splits the utility across two rules — `.line-clamp-2,.line-clamp-3{overflow:hidden;display:-webkit-box;-webkit-box-orient:vertical}` + `.line-clamp-2{-webkit-line-clamp:2}`. So the element is height-capped at 2 line boxes with `overflow: hidden` regardless of newline count ⇒ the `<ul>`'s `max-h-[45vh] overflow-y-auto` and `DialogContent`'s `max-h-[85vh]` are unaffected (no new scrollbars, no dialog growth, no horizontal escape). `pre-wrap` is clamp-compatible; `nowrap` would have broken it.
- **Corrected a stale assumption — `EmailDetail.tsx` is NOT a fourth site and must not be resurfaced as pending.** It was suspected from an earlier session, but `CitationCard` has rendered **no body text at all** since the 2026-07-26 compact-rows rework (title + inline category on one truncating line). Nothing to fix there.
- **Tests: +14 across 3 new per-concern files** (`PolicyList.newlines` 5 · `AddPolicyPanel.newlines` 5 — the **first tests for that component** · `PolicySelectPopover.newlines` 4, deliberately separate from the existing 25-test file). Setup follows the repo's **API-layer spy** convention (`vi.mock("@/lib/api")`, real hooks/debounce/Radix), and `toHaveStyle` (never `getComputedStyle`), matching the existing detail-view assertion. **All three mutation-verified with disjoint failure sets** — dropping `pre-wrap` fails only the style tests, dropping the clamp conditional fails only the clamp test, so neither is load-bearing for the other. Under the site-3 mutation the existing `PolicySelectPopover.test.tsx` stayed **25/25 green**, proving the two elements in that file are independent and that its detail-view assertion could never have caught this bug. Each file run individually; **full suite deliberately not run in these pieces**.
- **⚠️ jsdom performs NO layout, so no test in this suite can prove the newlines visually break** — the tests pin the `white-space` declaration and the clamp class, which is the strongest guard available here. Discovered concretely in 1a-ii: a `textContent` assertion **passes with or without the fix** (raw text nodes are layout-independent), so an early comment claiming it was the discriminator was wrong and was replaced with a `SCOPE LIMIT` note in each file. Only `toHaveStyle` catches the bug. Visual confirmation still needs a human — there is no browser driver on this host.
- **Flagged, explicitly NOT fixed** (separate decision, see Open Blockers): long unbroken tokens still do not wrap. Measured, not hypothesised — **24 tokens corpus-wide exceed 60 chars, longest 108** (Google Forms + AAAI PDF URLs, e.g. in `int_reciprocal-reviewer-changes`). Word-breaking is **orthogonal to `white-space`**: these tokens behaved identically before this change, so `pre-wrap` neither caused nor worsened it.

### 2026-07-29 (later) — Zendesk tag semantics: `ai_auto_replied` = verbatim public reply — MERGED+PUSHED to origin/main (`63e2dc2..b69cb3f`)
Redefined the `/send` tags by the AI draft's **fate**, not visibility. `ai_auto_replied` now means the AI's words reached the requester **untouched** → applied iff the reply went **public AND `draft.is_edited` is false**; `ai_drafted` = a chair-**edited** public reply **or** ANY internal note. One-line change in `emails.py` `/send` keying on the existing `draft.is_edited` (set by approve when final text differs from the AI draft; the automatic `[Sender name]` fill is part of the draft so it's NOT an edit; filling a `[CHAIR:]` placeholder counts). +4 tests (public×edited truth table); existing public/internal tests unchanged (their drafts are unedited). ⚠️ backend change — **needs a prod rebuild to go live** (not yet deployed).

### 2026-07-29 (later) — Chair Suggestions box → concise actionable workflow — merged to LOCAL main (`e611a07`), unpushed
Workflow-suggestion feature **v1 = prompt-only** (chosen after finding the "Chair Suggestions" box already renders `draft.notes_for_chair`). Rewrote the drafter's `NOTES FOR CHAIR` prompt so its content is a **short, ordered, actionable workflow** (what to look up/gather → what to decide, ≤5 steps, one imperative step per line → the box's `parseChairNotes` renders each as a list item) instead of terse per-placeholder gap-notes. **The "none" rule is kept verbatim**, so a complete draft still emits empty notes → the router's self-sufficiency floor / FAQ-lane gate (which keys on `notes_for_chair`, per E011) is **unchanged**. No code/DTO/frontend change. Live-verified: a `[CHAIR:]`-gap ticket → a 3-step workflow (routed human_review); a complete ticket → `none` (FAQ-eligible). Deferred alt: option B (a separate `chair_workflow` field so complete/FAQ tickets also get a workflow).

### 2026-07-29 (later) — Continual Experience Learning (CEL) — merged to LOCAL main (8 commits `ac83e4e..121094f`), unpushed · built via SDD
When a chair approves an edit that **resolved a `[CHAIR:]` gap**, a **non-blocking background task** learns from it: one `MODEL_PROVIDER`-gated LLM call judges generalizable-vs-one-off and (if generalizable) condenses a candidate **internal** policy; after lexical dedup (vs pending+rejected suggestions) and a `detect_conflicts` pass it persists a `policy_suggestions` row. Chairs review pending suggestions on `/knowledge-base` via the **existing add-internal-policy flow, pre-filled** (Check-for-related + conflict strip + edit + re-check + `retire_keys` + Create); **conflicts are surfaced, not dropped** (decision b). Accept reuses `POST /policies` then links via `PATCH /policies/suggestions/{id}/accept`; reject keeps the row (dedup reads it).
- **Components:** `app/pipeline/experience_learning.py` (gated judge+condense mirroring `policy_conflict`; **never raises** into approve), `SuggestionRepository` (CRUD + lexical `difflib` dedup over pending+rejected), `PolicySuggestion` model + migration `ed4fa4d333f2` (timestamps `nullable=False`), approve-hook (`BackgroundTasks`; fires only when `edited AND find_placeholders(original_text)`), endpoints (`GET /policies/suggestions[/count]`, `PATCH …/{id}/reject|accept` — declared BEFORE the `/{policy_key}` catch-all), frontend Suggestions section + pending-count badge (**`AddPolicyPanel` reused unforked**, pre-filled via the page's existing draft-lift).
- **Safety:** `MODEL_PROVIDER`-gated → no-op (`None`) without a real model, so no accidental model call in the approve path or tests; conflict candidates via a fresh **BM25** retriever bound to the injected session_factory (no embedding load, no prod-singleton mutation). Actor = `Chair1` placeholder until real auth.
- **SDD:** 6 tasks, per-task (spec+quality) reviews + whole-branch opus review ("ready to merge with fixes"); fixed the one Important — accept-mutation `onError` so a failed accept-link surfaces via the existing banner + is retry/reject-recoverable (no silent KB/suggestion divergence). **Full non-ml suite 608 passed / 7 skipped on the merged tree** (the earlier dev-SQLite `conflict_report` drift failures cleared once the migration caught the dev DB up); frontend tsc/next-lint clean, kb vitest 24/24 (full-repo vitest failures are the known node-25 jsdom/`react-dialog` env set, unchanged).
- **Deferred follow-ups (non-blocking):** `intents` not applied on accept (`POST /policies` has no intents field → shown read-only); `list_suggestions` N+1 email lookup; judge `policy_context` passes retrieved-ids not titles (anti-dup prompt line inert); add `_call_local`/`_call_anthropic` dispatch-branch tests; ticket link → native `<a>`. Design/plan: `docs/superpowers/{specs,plans}/2026-07-29-continual-experience-learning*` (gitignored).
- **Follow-on — suggestion review audit logging (mig `f8b2c4d6e0a1`; docs catch-up, implemented + merged in a prior session):** new **`suggestion_audit_logs`** table (`suggestion_id` [indexed], `action`, `actor`, `reason`, `resulting_policy_key`, `details` JSON, `timestamp`) + `SuggestionAuditLog` model + `SuggestionAuditRepository` (`app/repositories/suggestion_audit_repository.py`), wired into **both** review endpoints — `PATCH …/{id}/reject` and `…/{id}/accept` each write an append-only entry **after** the action lands. Exists because `PolicySuggestion.reviewed_by`/`reviewed_reason` hold only CURRENT state and are overwritten on every action; this table is the history they lose. ⚠️ **`suggestion_id` deliberately carries NO foreign key** — SQLite (local dev/test/CI default) does not enforce FKs without `PRAGMA foreign_keys=ON`, which nothing here sets, so an FK would be a Postgres-only guarantee the suite could never exercise; matches the existing convention on `policy_audit_logs.policy_key` and `policy_suggestions.source_email_id`.

### 2026-07-30 — Chair Suggestions panel: one box, plain rows, bullets, marker-strip, urgent rule (B1–D) · frontend-only
Jiacheng's request: the panel rendered each workflow step as its **own bordered, severity-tinted box**, so a 3-step workflow read as 3 unrelated alerts. Rebuilt as one box containing a bulleted sequence. Committed `d8b1a91` (SuggestionList newlines, separate) · `12dfab4` (B1) · `7f0dd60` (B2) · `6e3ff57` (C1); **C2 + D uncommitted** at time of writing. **40 chair-notes tests** in the new `EmailDetail.chairnotes.test.tsx`; every piece mutation-verified with disjoint failure sets. Suite **340 passed / 44 files**, tsc + lint clean. `parseChairNotes` and the `Collapsible` untouched throughout.
- **B1 — one outer container** (`ChairNotesPanel`): `role="group" aria-label="Chair suggestions"`, styled with the file's existing box convention (`rounded-lg border p-3` + `--border`/`--surface-raised`). Deliberately **neutral, not severity-tinted** — whether the outer box carries the amber/red identity is a later call. The **"Internal — not sent to the requester." caption stays OUTSIDE** the box (a disclaimer about the section, not a step) — pinned by a test so it stays a conscious choice. The bug the tests guard: a container rendered **inside** the `.map` (one box per note) is **invisible with a single note** — hence explicit 1/3/5-note cases.
- **B2 — per-row boxes stripped** (`ChairNoteRow`): dropped `rounded-md border-l-[3px] p-2.5 pl-3` + the inline `backgroundColor`/`borderColor` tint (and the now-dead `tint` const). Row spacing comes from the container's `space-y-2`. Two **independent** sweeps guard it — a class sweep and an inline-style sweep — because the tint was an inline style the class sweep cannot see; a partial revert of either kind is caught. The sweep reads **`getAttribute("class")`, not `className`**: SVG exposes `className` as an `SVGAnimatedString` that stringifies to `[object …]` and would silently exempt the icon.
- **C1 — bullet per row**: a muted `aria-hidden` **text glyph `•`**, deliberately NOT a `rounded-full` CSS dot — a dot needs `rounded`/`bg-` classes, which would trip B2's "container is the only box-level element" sweep and force an exemption into an otherwise-absolute guard. `aria-hidden` because the rows are `<div>`s, not `<li>`: an exposed glyph would be announced per step with no list semantics to earn it. Bullet leads the row; the icon still carries severity (both present — different jobs).
- **C2 — ⚠️ PRECAUTIONARY marker-strip, NOT a fix for an observed bug.** `stripLeadingListMarker` drops a leading `1. ` / `2) ` / `- ` / `– ` / `— ` / `* ` / `• ` so a model-emitted marker cannot render as `• 1. Do X` beside C1's bullet. **No prefixed multi-step note has ever been seen.** Evidence hunt found *zero* real note text on this machine (dev SQLite has 0 email rows, no docker, `data/eval_real/` absent, `pipeline_trace.jsonl` + all 20 `backend/reports/*.json` contain zero `notes_for_chair`); **two live probes** against the drafter (`gpt-5.6-terra`) settled nothing — the second returned a **single-step, unprefixed** note (`"Determine whether withdrawal and resubmission … is permitted for paper #4127."`), so the multi-step case remains **unobserved**. Shipped anyway because the drafter prompt says "one imperative step per line" but never forbids numbering, `MODEL_PROVIDER` is a documented swappable seam, and the guard is inert absent a marker. **If you ever see doubled markers, that is the FIRST observation — record it.**
- **D — urgent rows regain a distinction, as a RULE not a box.** B2 had left severity carried only by the icon; D adds a **2px `--danger` left rule** on urgent rows. Diagnosis worth keeping: urgent already had *three* signals advisory lacked (octagon vs triangle glyph, `--danger` vs `--warning`, an extra "AUTOMATED LEAK CHECK" label) — what it lacked was anything **breaking the panel's uniform left-edge rhythm**, which is what a chair scanning vertically actually follows. A `--danger-subtle` background tint was **rejected on measured contrast**: dark-mode `#2b0f0f` on `--surface-raised` `#21263a` is barely separable (light `#fdeaea` on `#eef0f6` likewise), so it would have been the *least* scannable option. Strictly lighter than what B2 removed — 2px not 3px, no tint, no rounding, no per-row padding block. **Advisory rows carry the same border at `transparent`** purely for alignment (an urgent-only border would shift that row 2px and make the list jitter); advisory rendering is pixel-identical to pre-D. Bullet also tightened to the icon via `-mr-1` (~6px bullet→icon, icon→text stays 10px) — it had read as a third free-floating element under the uniform `gap-2.5`.
  - **⚠️ jsdom cannot expand a shorthand containing `var()` into longhands** — `style.borderLeft` reads back `"2px solid var(--danger)"` while `style.borderLeftColor` reads `""`. This silently made B2's original longhand sweep (`borderColor === ""`) **vacuous**: it would have passed against a full `border: 1px solid var(--x)` box, the exact regression it existed to catch. Rewritten to match **cssText** against `/(^|;)\s*(background|border(?!-left))/` — forbids any background and every border edge except left, and is mutation-proven to catch a re-introduced tinted box. **Assert inline `var()` styles on the shorthand, never the longhand, anywhere in this codebase.**
  - **Regex `/^(?:\d{1,2}[.)]|[-–—*•])\s+/`, three narrowings each mutation-proven load-bearing** (disjoint failures): mandatory **`\s+`** is the whole discriminator separating `- Ask the chair` from `-5 degrees` / `1.5x` / `*emphasis*` (relaxing to `\s*` fails exactly 3 keep-cases); **`\d{1,2}` not `\d+`** so a year-leading line — `2026. Deadline moved to March.` — is never mangled into `Deadline moved to March.` (widening fails exactly that 1); `^`-anchored only, single application (no loop), and an **empty-result guard** so a bare `1)` keeps itself instead of rendering an empty row. The **live-probe string is pinned as an explicit no-op keep-case**, so the guard provably never touches the one real output we possess.

### 2026-07-31 — `received_at` recorded INSERT time, not the requester's arrival time — fix on `fix/received-at-source-timestamp` (3 commits) + prod backfill script (built, NOT run)
`Email.received_at` carries only `server_default=func.now()`, so it recorded **when our poller inserted the row**, never when the requester opened the ticket. The correct value existed all along one column over: `zendesk_created_at` has been populated from `ticket.created_at` on every new-ticket ingest since mig `d2e4f6a8b0c1`, independent of whether `received_at` was right — it was simply never wired through, and is still serialized by **no** endpoint. A dead `"timestamp": ticket.get("created_at")` sat in the adapter's `email_data` and was dropped at the `_compute` record boundary.
- **Fix — all four ingestion paths** (`d240962`-line branch, 3 commits): `orchestrator._compute` now parses `email_data["timestamp"]` into `record["received_at"]` via new `_parse_received_at` (commit A) — one choke point, so the Zendesk full-pipeline branch AND manual `/ingest` are both fixed by it; the adapter's two **direct-`create_email`** branches (closed/archived, bare-pending) pass `_parse_dt(ticket["created_at"])` inline (commit B); commit C normalizes to UTC. Key added **CONDITIONALLY** so it stays **ABSENT, not None** — `reprocess_email` / `reprocess_email_with_thread` carry no `"timestamp"` key, so an existing row's `received_at` is never re-stamped (`update_email_outputs`' allowlist is a second guard). **656 non-ml passed** (+37).
- **⚠️ Storage is NOT offset-preserving (commit C, a real defect found by a failing assertion):** SQLite's `DATETIME` writes wall-clock fields and **silently discards `tzinfo`**, so `14:45-05:00` persisted as `14:45` instead of `19:45` — wrong by 5h, no error, SQLite only (asyncpg converts correctly for `timestamptz`). `_parse_received_at` now attaches UTC to naive input **first** (`astimezone` on naive assumes system-local) then converts. No-op for the `Z` timestamps Zendesk sends.
- **Two parsers, deliberate asymmetry:** the adapter reuses `_parse_dt` (**raises** on malformed, as it already did for `zendesk_created_at` — safe, `sync` wraps each ticket in a try/except and the raise now precedes `create_email` so no orphan row); the pipeline's `_parse_received_at` **logs and degrades** to insert time (never fail a run mid-draft). Pinned by test so neither drifts.
- **Corrected an earlier claim in-tree:** comments in both files said an explicit `None` "would INSERT NULL into a NOT NULL column". **False** — SQLAlchemy 2.0.51 omits a None-valued attribute on a `server_default` column from the INSERT, so the default still fires (verified). The conditional is **defensive, not load-bearing**; comments now say so.
- **Prod impact (read-only discovery, API + SQL):** **3,231 rows** would change, all `source='zendesk'` (prod has **zero** toy rows, so the "skip toy NULLs" risk is retired — the `IS NOT NULL` guard still matters for pre-migration rows). Every row's `received_at` sits in one **11-day window (Jul 21–31)**, **~1,690 on Jul 21 alone** (the initial bulk sync, paced ~15/min), while real tickets reach back to **2026-01-02**. Drift is **bimodal**: ~50% live-polled at 1–6 min, the rest weeks-to-months, **max 972 days**; 4 outliers ≥400d hand-verified as legitimate old-cycle AAAI tickets. 0 rows would become NULL, 0 `received_before_created`.
- **`scripts/recovery/backfill_received_at.py`** — same caution pattern as the internal-notes scripts: dry-run default (+ PG `SET TRANSACTION READ ONLY`), `--execute` + typed `yes` (or `--yes`), rollback JSON (`id` + prior value) **fsynced BEFORE the UPDATE**, rowcount+drift verified **inside** the transaction (commit only if clean). Buckets computed in Python (dialect-agnostic/testable); predicate uses `is_distinct_from` (a plain `!=` is NULL-blind and undercounts).
- **⚠️ The column guard caught a real bug, not a hypothetical.** `Email.updated_at` is `onupdate=func.now()`, so SQLAlchemy **silently appends `updated_at=CURRENT_TIMESTAMP`** to the SET clause — the backfill would have restamped `updated_at` on all 3,231 rows, destroying the forensic signal a prior incident used to prove a sweep had *not* written to a row (Workstream A). Fixed by pinning `updated_at` to itself; the guard now permits it **only** in exact self-assignment form. Unfindable by reading the source — the ORM adds it at compile time.
- **Tests +68 total**: `tests/test_received_at_source.py` (**37** — one per ingestion path asserting the SOURCE timestamp not "now", fallback matrix, reprocess invariants, absent-vs-None record contract, parser matrix) + `tests/recovery/test_backfill_received_at.py` (**31**, house rule 5). All mutation-verified with near-disjoint failure sets; the ordering and verification properties each fail exactly one test. Suite **687 non-ml passed / 7 skipped**; the 3 failures are pre-existing env drift (`alembic` absent from `.venv`; +10 more with `ml` from missing `sentence_transformers`) — identical with the changes stashed.
- **STATUS: backfill NOT RUN on production.** Awaiting a prod dry-run — **expect exactly 3,231 rows and 4 outliers; if either differs, stop.** No DB access from this machine (5432 unforwarded, SSH to prod blocked). Note it retroactively changes visible surfaces: queue default order is `received_at DESC`, and the analytics 7-day `daily_volume` empties as the Jul-21 rows scatter back to Jan–Jul.
- **Parked: Piece 3 (date/conference filtering) remains unscoped** — to be picked up once this backfill is decided/run, since it depends on `received_at` being trustworthy.

### 2026-08-01 — Date-range filtering on the queue (`received_after`/`received_before`) — shipped, backend + frontend
Ida asked whether tickets can be filtered by date. Built on the `received_at` fix above (the column is only trustworthy because of it). **Piece 1 of the parked "date/conference" item; conference is still parked — see the last bullet.**
- **Backend** — two nullable params on `GET /emails/queue` AND `GET /emails/queue/facets`. Both flow through the SAME `_queue_conditions`, so the page and its `total` can never disagree, and through the same route-level `_received_range` helper, so the two endpoints can never resolve a window differently (the counts beside the list would otherwise describe a different set than the list). Dates are **context filters, not facet dimensions** — a window narrows the status-bar/source counts exactly as `status`/`search` do, while `sources` stays whole-table.
- **Boundary contract (route-level, deliberately not the repository's)**: a **bare `YYYY-MM-DD` covers the WHOLE day** (`received_after`→00:00:00, `received_before`→23:59:59.999999); a value carrying an explicit time is used **verbatim**, never re-widened; naive input is read as **UTC**. "Date-only" is decided on the **RAW TEXT**, which is why the params are typed `str` and not `datetime` — FastAPI would coerce `2026-01-20` to midnight and destroy the distinction the whole fix depends on. An inverted range is a **422** naming both resolved values (otherwise indistinguishable from a genuinely empty window); malformed input is a 422 too, never a silently-unfiltered queue.
- **⚠️ Deliberately does NOT fall back to `zendesk_created_at`** for non-Zendesk rows. That column is NULL for `/ingest` and toy rows, and under SQL three-valued logic every comparison against NULL yields NULL — such a "helpful" fallback would silently drop those rows from every range query. `received_at` is NOT NULL on every row, which is what makes it safe to compare directly.
- **Frontend** — first date library in the project (`react-day-picker` + `date-fns`, via `npx shadcn add calendar`). `components/ui/calendar.tsx` needed **two** independent rewrites: color (stock `bg-muted`/`text-primary-foreground`… reference CSS vars this project doesn't define — same hand-wrap as button/dialog/dropdown-menu/popover) **and Tailwind version** (the registry emits v4 syntax — `size-(--cell-size)`, `--spacing(7)`, `in-data-[…]` — which compiles to NOTHING on this project's Tailwind 3.4, so cell sizing and radii silently vanished). `DateRangeFilter.tsx` = trigger + presets rail + calendar, wired into `EmailWorkspace`'s existing pattern (`usePersistedState` under `confmail.filterReceived*`, `filterKey`, `contextParams`) and rendered in `QueueFilterPanel` below the Zendesk status bar.
- **⚠️ `npx shadcn add` SILENTLY OVERWROTE `button.tsx`** — reverting its hand-wrapping AND swapping its underlying lib (`@radix-ui/react-slot` → `@base-ui/react/button`). Caught from the CLI's own "Updated 1 file" line and reverted. **Always `git status` immediately after a `shadcn add` in this repo.**
- **Apply/draft flow, added after manual testing** — the first version committed on every calendar click, which made range-building unusable (picking a start date fired a filter change and closed the popover, leaving no room to navigate months). Now **one rule governs the whole popover: nothing commits until Apply** — presets fill the draft too, for that consistency. The draft re-seeds from the committed value on every open, so Escape/click-away discards cleanly. Header "Clear" (outside the popover) stays an immediate commit; footer "Reset" is a draft action.
- **⚠️ Bug worth remembering — an explicit child color silently beats an inherited parent rule, whatever the specificity.** Outside-month days rendered **identical** to in-month days: `outside: text-[var(--text-muted)]` sat on the `<td>`, but `CalendarDayButton` sets its own explicit `text-[var(--text-primary)]`, and an explicitly-set color always wins over an inherited one. Fixed by targeting the button (`[&_button]:text-[var(--text-muted)]`, descendant selector 0,1,1 > 0,1,0). Diagnosis mattered more than the fix: the request was "make them dimmer", but no color change would have done anything while the rule wasn't applying. Selection/focus states still correctly override it.
- **⚠️ Timezone, on the client this time** — both obvious conversions are wrong: `toISOString().slice(0,10)` serializes in UTC (a user east of UTC picking the 20th sends the 19th), and `new Date("2026-01-20")` parses as UTC midnight (west of UTC the calendar highlights the 19th — **confirmed on this machine at UTC-7**). Everything goes through date-fns `format`/`parse`, which are local. Companion to the SQLite-drops-`tzinfo` defect in the entry above: the same class of bug, once on each side of the wire.
- **Tests ~+100**: backend `test_queue_date_filter.py` (**27** — whole-day boundary incl. a 23:50 ticket that midnight-coercion drops, explicit-timestamp-not-widened, 422s, blank-is-absent, both endpoints resolving one window) · frontend `DateRangeFilter.test.tsx` (**23**) + `queue-date-filter-wiring.test.tsx` (**7**) + calendar coverage. All mutation-verified. **⚠️ The `filterKey` regression test was structurally impossible until the control was rendered** — `filterKey` only resets the page on a MID-SESSION change, so it must be driven through the real control; a mutation removing the dates from `filterKey` caught nothing until then. Gates: backend **714 non-ml passed / 7 skipped** (3 failures = pre-existing `alembic`-missing env drift); frontend **374 passed / 46 files**, tsc + lint + build clean. Temporary `/probe-cal` visual-review route deleted.
- **Parked: conference filtering (Piece 2) remains UNSCOPED** — there is no conference concept in the schema at all, and the adapter reads **none** of Zendesk's `tags` / `custom_fields` / `ticket_form_id` / `brand_id` (`_ZENDESK_FIELDS` has no slot for them). Blocked on a **live read of a few real tickets** to see whether AAAI's Zendesk actually populates any of them with a conference signal — not yet done. That answer decides between *derive from an existing upstream field* (add to `_ZENDESK_FIELDS` + re-sync) and *invent our own column* (migration + a population rule). `received_at` is a weak fallback proxy (cycles are time-bounded) but cannot separate two concurrent conferences.

### 2026-08-05 — Reply-visibility toggle removed; visibility now FIXED per submit status — committed + pushed to origin/main, **NOT deployed**
Marc/AAAI asked for the chair-facing public/internal switch to go. It was a mode the chair had to remember to set correctly on every ticket, and getting it wrong was silent in both directions — an intended public reply landing as an invisible internal note, or an internal note reaching the requester. Visibility is now a pure function of the status the chair already picks: **Solved → always public · Pending / Open → always internal**. Frontend-only behavior change (4 commits, plus a docs-only 5th); no pipeline module, no backend logic, no migration.
- **Mechanism**: one module-level `REPLY_PUBLIC_BY_STATUS: Record<SplitActionStatus, boolean>` in `EmailDetail.tsx`, read at BOTH call sites — the split button's `onAction(status)` and the Ctrl+Alt+S shortcut (which reads the persisted `approveStatus` instead, a genuinely different variable, so the two could otherwise drift). Typed as a `Record` over the union on purpose: a future 4th status fails to compile rather than silently defaulting to internal.
- **Soft-removed, not deleted**: `SendVisibilityToggle.tsx` is **untouched on disk**; its import, its JSX, and the `replyPublic` `usePersistedState` are commented out in place with inline restore notes. Restoring = uncomment those three + point the two call sites back at `replyPublic`. ⚠️ On restore, note the persisted `confmail.replyPublic` key may still hold a stale `true` from before this change.
- **⚠️ The safety property here is NOT what it looks like.** `ALLOW_AUTO_SEND=False` is **not** a backstop for this: `send_gate.authorize_send` authorizes any **chair-approved** draft regardless of the flag (pinned by `test_approved_public_allowed_without_flag`), and `/send` applies no second visibility gate. Since the UI approves *then* sends, **"Submit as Solved" now posts a customer-visible Zendesk reply in production**, with the status choice as the only guard. Per CLAUDE.md history the public path has **never been live-verified** — only internal notes have (ticket 21567). Verify on a safe ticket before this reaches chairs.
- **Side effect (expected, not a defect)**: unedited Solved replies now get Zendesk tag `ai_auto_replied` instead of `ai_drafted`, since `emails.py` keys that tag on `public and not is_edited`. Semantically correct per the 2026-07-29 tag rework — just a visible change in the tag list.
- **Docstring corrections alongside** (comments only, zero logic): `SendRequest.public` claimed it "needs ALLOW_AUTO_SEND" in **both** `emails.py` and `types/index.ts` — false since the send-gate decouple, and exactly the misreading that would make someone think the flag protects them here. `SendRequest.target_status` was documented "reserved; currently unused" though it IS consumed. Both `description=` strings were corrected too (they render in `/docs`). **`ApproveRequest.target_status`'s "not consumed by the backend" note is still ACCURATE** — backend `ApproveRequest` really has only `approved_by`/`final_text` — and was deliberately left alone.
- **Tests**: `approve-send-chain.test.tsx` rewritten (9 → 14 tests). Old test 3 clicked `getByRole("switch")` and is gone; the coverage gap the investigation flagged is closed — **Pending and Open payloads had never been exercised at all**, only Solved. Now two `it.each` tables cover all 3 statuses × both call sites, plus a Solved-stays-public-after-an-edit case and a guard asserting **no visibility control renders** (catches the toggle being restored without its call sites). Mutation-verified with disjoint failure sets: map→all-false fails only the 3 Solved tests, →all-true only the 4 Pending/Open tests, and hardcoding the shortcut call site fails only its own test (proving the two paths are independently covered). Gates: frontend **379 passed / 46 files**, `tsc` + `next lint` clean; backend untouched.
- **Committed and pushed** to `origin/main` (`8b85426` map added unwired · `5fc874e` wired to both call sites + toggle soft-removed · `7e19a0a` tests rewritten · `d0e8a30` docstring corrections · `a1534ca` this entry). **⚠️ NOT deployed** — the production push is still pending, and per the caveat above it must not go out until **Marc and Ida have been warned that "Submit as Solved" will post a customer-visible reply**. Frontend-only, so no `docker compose build backend` is needed, but the frontend image bakes at build time ⇒ **`docker compose build frontend` is mandatory** for it to appear (the recurring deploy trap in this project — `git pull` alone is never sufficient).

### 2026-08-06 — Paper ID + author extraction ("Submission Details" panel) — Complete · 11 commits `a6ef245..0ce1307` · backend + frontend
Pulls the submission an email is about, and the people it names, out of the email text and shows them to the chair: a **"Submission Details"** panel in the email detail view, between the Intent row and the conversation — submission number, a link to the OpenReview forum page, and author mentions (name / email / affiliation, each independently optional). The panel **self-suppresses**: a null extraction, or one that examined the email and found nothing, renders NOTHING — there is no empty state, so a ticket with no identifiers looks exactly as it did before.
- **Two paths, MUTUALLY EXCLUSIVE BY DESIGN — a locked decision, not an oversight.** When the distiller runs (`QUERY_STRATEGY=distill` + `MODEL_PROVIDER=local`) it reports the identifiers on NEW output lines (`SUBMISSION_NUMBER:` · `OPENREVIEW_ID:` · repeatable `AUTHOR: name | email | affiliation`) alongside the existing INTENT/CONFIDENCE/QUERY contract, so there is **no second model call**. Only when the distiller did NOT run does a regex fallback read subject+body+sender. Regex is **never a supplement** topping up a present LLM result: an empty LLM answer is an ANSWER (the prompt directs the model to read both subject and body and answer NONE), and merging the two would need per-field provenance to stay honest. `method` therefore stays a clean 3-value enum (`llm_distiller` / `regex_fallback` / `none`). **Do not "fix" this into a union later without adding provenance first.**
- **⚠️ SCOPE BOUNDARY — text-only extraction. There is NO OpenReview API integration**: no lookup, no verification against the system of record. The values are whatever the email says. On the **regex path specifically, "Authors" is only the email's SENDER**, who is not necessarily a verified author of the paper (a code comment at the label says so). The forum link is built from the extracted id and is never checked to resolve. This is the natural place to extend **if/when OpenReview API access becomes available** — a future possibility, not a current gap to fix.
- **The QUERY contract was NOT touched.** The distiller's "never include … paper ids, paper titles, years" rule still governs QUERY lines (identifiers are noise against a policy corpus, per E003); the identification lines are separate, introduced by an explicit scoping sentence so the model cannot read that prohibition as covering them.
- **Persistence + API**: `extraction` JSON on BOTH `emails` and `email_processing_results` (mig **`57b59f3ef990`**, additive nullable, `batch_alter_table` both ways — **downgrade actually exercised on SQLite AND Postgres**, indexes/rows/constraints intact, in a throwaway DB, never the demo one). Wired at ONE choke point, the record dict in `orchestrator._compute`, which every entry point funnels through (`process_email` / `reprocess_email` / `reprocess_email_with_thread` / the public `compute` seam used by per-follow-up processing) — so both follow-up paths got it for free with no duplicated call. Served verbatim by `_email_to_dict`.
- **⚠️ Two identifiers, two different hazards — do not conflate them.** `submission_number` (4–5 digits) needs a cue word (`paper`/`submission`/`#`) and must reject a number adjacent to a conference designator (`AAAI-26`, `AAAI 2026`) and a year reached across a filler word (`paper due 2026`) — on real traffic 239 year-tokens sit within 40 chars of paper/submission. `openreview_forum_id` is exactly 10 mixed-case alphanumerics and must anchor to `/forum?id=` or `/pdf?id=` ONLY: a bare `?id=` also matches AAAI's committee URLs (`/group?id=AAAI.org/2026/Conference`), a confirmed real false positive. Subject is scanned before body (~28% of real submission numbers sit in the subject, usually a quoted AAAI notification).
- **⚠️ NULL vs examined-but-empty is a PERSISTED SEMANTIC, not a display detail.** `extraction = NULL` means "never examined" (the row predates the feature); a present object whose `submission_number` is null means "examined, found nothing". These must not collapse at any layer — the DB comment, the API passthrough (null never coerced to `{}`), and the frontend type all state it, and mutation tests at the API boundary fail if either coercion is reintroduced.
- **⚠️ "Mutation-verified" sometimes means "found and fixed DURING the pass", not "confirmed after".** In the authors piece **3 of 13 mutations initially SURVIVED** — not because the code was wrong but because the tests were genuinely weak: a blank-name case was never exercised; an all-empty-mention guard was asserted via `textContent`, which an empty `<span>` does not change; and "stacked, not inline" was asserted by child count, which cannot tell the two apart. All three were real coverage gaps, fixed and re-verified. Treat a clean first mutation pass as the exception, not the expectation.
- **Files**: `distiller.py` (output lines + raw `DistillResult` fields) · **`extractor.py` (NEW)** — `AuthorMention` / `ExtractionResult` / `EmailExtractor`, LLM-path normalization + regex fallback, never raises · `orchestrator.py` (one call in `_compute`, deliberately OUTSIDE the classifier tracer stage so no existing stage's timing moves) · `models.py` + mig `57b59f3ef990` · `schemas.py` + `api/v1/emails.py` · `types/index.ts` · **`SubmissionDetails.tsx` (NEW)**, wired into `EmailDetail.tsx`.
- **Gates**: backend **819 non-ml passed / 7 skipped**; frontend **426 passed / 48 files**, `tsc` + `next lint` clean. Every piece mutation-verified.

---

## Current Status — Phases 0–6C COMPLETE · Real-Corpus + Phase 7 COMPLETE · Zendesk Pieces 1–5 COMPLETE + proven live end-to-end · Queue status bar + KB E007 (drop policy tags) merged · post-merge tree 273 passed / 7 skipped (`-m "not ml"`) · frontend 11 vitest passed · `feature/production-hosting-v2` 192/192 · frontend build clean
| Phase | Status | Summary |
|---|---|---|
| 0–2 | Complete | Scaffold/config/DB/frontend shell · data+pipeline+v1 API · full Next.js frontend |
| 3 | Complete | audit endpoint · postgres-ready · local drafter · trainable classifier · RL router |
| 4 | Complete | FAISS retrieval · eval harness · progress PDF |
| 5 | Complete | tracing · calibration · fusion · template drafter · SSE queue+calibration view · chair-edit diff+shortcuts · active-learning flag · adapter spec · Docker(SQLite)+CI |
| 6A/6B/6C | Complete | multi-chair routing (11 intents, chair_router, reassign) · frontend · paginated-aggregate bug-class fix |
| Real-Corpus + 7 | Complete | 93-chunk real AAAI-27 corpus (56-chunk archived) · query distiller (`QUERY_STRATEGY`) · placeholder reply contract · send gate (`ALLOW_AUTO_SEND`) · style guide v2 · hermetic conftest · Zendesk groundwork |
| Today | Complete | style_guide_v2 committed default (`c4ed3f5`) |
| PG migration | Complete · on `main` | SQLite→Postgres now on `main`: Docker `db` service (loopback, healthcheck) · single-source `DATABASE_URL` · `SYNC_DATABASE_URL` removed · dialect-agnostic JSON (`json_extract` fix, both sites) · PG test suite (`test_postgres_migration.py`, skipif w/o `TEST_DATABASE_URL`) + CI Postgres · `config.py` default stays SQLite (local/test/CI fallback) · `external_api` excluded by design |
| Re-eval on policy change | Merged to main (2026-07-19) | `feature/reevaluate-on-policy-change`: "Re-evaluate open tickets" button → sweep re-drafts only tickets whose retrieval set shifted (no model call in the gate) · `emails.retrieval_context`+`redrafting` (mig `c1d2e3f4a5b6`) · atomic claim, null-context skip, startup stale-flag recovery · 211 non-ml pass |
| Retrieval rework (E005) | Merged to main (2026-07-19) | `retrieval-rework` (off main): embed **leaf title** not full path (`faiss_retriever._embed_text`) — E005 real-gold A/B, dense hit@1 .514→.649 / fusion hit@1 .649→.703, no regression; dense-embed-only (title/BM25/citations unchanged). Design menu in `docs/local/RETRIEVAL_REWORK.md` |
| Zendesk Pieces 1–3 | Complete · migration applied | OAuth credential provider (`ZENDESK_AUTH_MODE`) · write-scope confirmed (ticket 22009) · ticket data model (`Email` extended + `email_thread_messages`, migration `d2e4f6a8b0c1` applied to demo Postgres, 47 emails intact) · local 207/207 · no pipeline module touched |
| Zendesk Piece 4 | Complete · migration applied · **first live sync ✓** | Read-only ingest adapter (incremental cursor export → upsert by `zendesk_ticket_id` → thread messages → classify-once via `process_email`) · shared `run_sync_cycle` behind manual `POST /api/v1/zendesk/sync` + gated background loop (`ZENDESK_POLLING_ENABLED=False`) · `zendesk_sync_state` migration `e3f5a7c9b1d2` applied · local 217/217 · **first live read-sync 2026-07-19: 5 Zendesk sample tickets → `source='zendesk'` rows, classified, 0 errors, cursor advanced; 5 rows retained for Piece 5 testing** |
| Zendesk Piece 5 | Complete · **proven live e2e** | Write-back on the EXTENDED `POST /emails/{id}/send` (source-branching, not a parallel route) · transport `sender.py` (`ZendeskSender`) · internal note default / public reply only w/ `ALLOW_AUTO_SEND=True`+explicit (§4) · closed-ticket 409 · tags via `safe_update`/`updated_stamp` (409 surfaced, not overwritten) · Zendesk failure → `SEND_FAILED` (re-triable) · `POST /zendesk/sync` gains `max_pages`/`per_page` · redraft-on-followup = option (a) · **live e2e PASSED on real ticket 21567 (internal note, status unchanged, ai_drafted tag)** |
| Zendesk overlap guard | Complete · **migration now applied to demo** | Single-flight lock on `zendesk_sync_state` (`is_running`+`running_since`, `try_acquire_lock` w/ `FOR UPDATE`) · 2nd trigger → 409 "already in progress" · 900s staleness takeover for crashed runs · migration `f7a1b2c3d4e5` **APPLIED to demo Postgres 2026-07-19** (side effect of a frontend rebuild recreating backend; additive columns, 47+6 rows intact; demo now at head `f7a1b2c3d4e5`) · local 265/265 |
| Queue status bar + source toggle | Complete | Dedicated facets aggregate `GET /emails/queue/facets` (`count_queue_facets`, no capped-page tally) + server-side `source`/`zendesk_status` queue filters (compose with lane/chair/search) · `ZendeskStatusBar` (clickable status counts) + self-hiding `SourceToggle` (vanishes when <2 distinct sources) · first frontend test infra (Vitest + RTL, 11 tests) · backend 274/6 (+9 `test_queue_facets.py`) · tsc clean |
| Drop policy tags (E007) | Complete · on `kb_enhancement` | Auto-gen policy tags carry no retrieval signal (E007 ablation — flat at BM25+fusion) → `policy_documents.tags` dropped (mig `e7a9c1f2b3d4`, down re-adds), app-side tag path commented-out/restorable (`[tags-dropped E007]`) · 270 passed/7 skipped · frontend tsc clean |
| Intent taxonomy + FAQ-lane rework | Merged to main (2026-07-20) | 14-intent taxonomy single-sourced in `taxonomy.py` (coverage map; intent prior gated OFF per E010) **+** FAQ lane decided by DRAFT QUALITY not intent: drafter self-rated `answer_confidence`, route-after-draft, `FAQ_ELIGIBLE_INTENTS` retired, `SENSITIVE_INTENTS=[]` seam, strategy-independent self-sufficiency floor, chair reseed mig `b2c3d4e5f6a7` (single head), run_eval end-to-end (E011). Post-merge suite 303 passed/7 skipped (non-ml) · 325 incl ml |
| `.env.example` cleanup (pre-deploy) | Complete · NOT committed (Sahil to commit) | Docs-only, zero runtime change (`config.py` untouched). Deleted stale root `.env.example` (backend section superseded); created `frontend/.env.example` (preserves `NEXT_PUBLIC_API_URL`, corrected `/api/v1`); README frontend `cp` pointer fixed; documented `ALLOW_AUTO_SEND=False` + `ZENDESK_POLLING_ENABLED=False` (must-stay-False warnings) and recommended `ZENDESK_AUTH_MODE=oauth` (code default stays `token`) in `backend/.env.example`. Guard test 2 passed |
| Deployment hardening (internal-only) | Pieces 1&2 merged to main · Piece 3 (README) uncommitted | `docker-compose.prod.yml` override rebinds backend/frontend to `127.0.0.1` (`ports: !override` — plain list concatenates + leaves 0.0.0.0 open; verified via `docker compose config`) · `NEXT_PUBLIC_API_URL` build arg now `${NEXT_PUBLIC_API_URL:-…localhost…}` (build-time baked; rebuild required) · README "Production Deployment" section (loopback-only = server-only/SSH-tunnel, not public; reverse proxy/TLS out of scope). Base file + local dev unchanged |
| Ticket badge + status-select approve (Piece D) | Complete · **UI-only** | `zendesk_ticket_id` exposed end-to-end (`_email_to_dict` + `Email` type) · `#{ticket}` badge in list + detail (null-guarded) · new `SplitActionButton` (Open/Pending/Solved dropdown, Radix DropdownMenu wrapped to app tokens, `loading` prop) · shared `lib/zendesk-status.ts` color source (reconciles `SplitActionButton`↔`ZendeskStatusBar`) · `target_status` threaded through frontend approve payload w/ explicit TODO — **backend does NOT consume it yet (blocked on per-chair OAuth send)** · tsc clean · vitest 11/11 |
| Multi-turn threads + follow-up reprocessing | Merged to main (2026-07-21) | `feature/multi-turn-threads-followup` (SDD): `GET /emails/{id}/thread` + `useEmailThread` + `ConversationThread` multi-turn UI (replaces cramped monospace box) · whole-thread transcript → distiller/drafter · reprocess-on-followup (unless `closed`) re-drafts + `draft.history[]` + `reprocessed_on_followup` audit · NO migration · **reconciled w/ Sahil's `EmailProcessingResult` (kept DORMANT) + his T3b/T4 superseded by ours** · 351 non-ml pass / tsc clean / head `af44a007c60f` |
| Workstream A — resolved-ticket guard on policy sweep | Complete · commit `a367c0c` · **live-verified** | Policy sweep re-drafted Zendesk tickets already `solved`/`closed` (checked local `Email.status` only, never `zendesk_status`) — **confirmed in the wild: email 205 re-drafted 2026-07-20 16:55 per audit trail**. Fix = `_RESOLVED_ZENDESK_STATUSES` guard in `get_open_tickets` ONLY (new constant, deliberately not reusing display-only `_SOLVED_BUCKET_STATUSES`; `IS NULL` branch load-bearing vs SQL 3-valued logic) · **area 4 follow-up reprocess confirmed correct as-is, no change** (Zendesk auto-flips solved→open on customer reply; audits.json check optional, not a blocker) · +8 tests, mutation-verified (359/7 non-ml) · live: `open 46 not 58`, 12 rows byte-identical, probe proves sweep still works · ⚠️ **requires `docker compose build backend`** — running image predated the fix |
| Workstream B — Approve→Send chain (visibility toggle + `target_status`) | Complete · **live-verified** · committed `7e0727b`..`6d7e87a` | Frontend "Approve & Send" now reaches Zendesk: added `sendEmail()`/`useSendEmail` (didn't exist), `SendVisibilityToggle` (default internal), chained approve→send, retryable partial-failure banner (approve never rolled back). Backend `SendRequest.target_status` (open/pending/solved) → `ZendeskSender.set_status`, byte-identical fallback when unset. +6 backend (365/7 non-ml) +7 frontend integration (20 vitest, tsc clean). **Live e2e on ticket 21567 (internal notes only, Zendesk-confirmed)**: Test 1 status unchanged, Test 2 `new→pending` — no public comment, `ALLOW_AUTO_SEND=False`. ⚠️ public path NOT yet verified; needed `docker compose build backend` |
| Light/dark theme toggle | Complete · frontend-only · **NOT committed** (Sahil to commit) | Sun/Moon pill switch in Sidebar footer; **defaults dark (zero change until opt-in)**. No `next-themes` — additive `:root[data-theme="light"]` block in `globals.css` (dark `:root` untouched) + `useTheme` hook + `localStorage` `confmail-theme` + inline anti-flash `<script>` in `layout.tsx` `<head>` (no flash on hard refresh). ~95% of app themes via inline `var(--token)`, so one block recolors it; recharts is the exception → theme-conditional `CHART_PALETTE` in `analytics/page.tsx`. Fixed real light-mode bugs: `CHAIR_PALETTE` a11y (pastel badge TEXT ~1.5–2.6:1 on white → darker hue set, `chairColor(id, theme)`), `DiffView`/calibration-caveat inline rgba (→ T2-token variants); `zendesk-status`/`LiveStatusDot` dots verified fine (label-paired), left unchanged. `tsc`/`lint` clean, manual both-theme pass across all views |
| Approve shortcut Ctrl+Alt+S + shortcuts popover | Complete · **live-verified** · frontend-only | Approve moved off plain `A` → **`Ctrl+Alt+S`** (E/C/R unchanged); hint row → `ShortcutsHintPopover` (Keyboard-icon trigger, visible at ALL widths; the old row was hidden <640px and crowded the action buttons above it). **⚠️ AltGr bug class caught**: on non-US layouts AltGr **is** Ctrl+Alt, so the initial typing-guard bypass would have made typing in the draft accidentally approve-and-send → combo now respects the typing guard (guard hoisted to cover every shortcut); matched on `e.code==="KeyS"` (layout-independent); no `preventDefault` when not actionable. `Kbd` relocated to `components/ui/`; new dep `@radix-ui/react-popover` + themed `popover.tsx` (true popover, not the dropdown-menu — non-interactive content). 380/7 backend · 20 vitest · tsc/lint clean |
| Nav rail + collapsible filter column (N2–N6) | Complete · frontend-only | Three-column layout: **icon rail** (52px `--rail-width`, icon-only, radix-tooltip labels, theme toggle in footer) · **page-owned filter column** (`PanelLeft` toggle, 256↔52px animated, persisted `confmail.filterColumnCollapsed`) · **drag-resizable list** (viewport-clamp vs dynamic `reservedWidth`, so collapse hands freed space to detail) · **detail** `flex-1`. Removed the `SidebarSlot` portal (deleted; regression-guarded). Z-index settled: rail 40 / drawer 30 / radix+modal 50 / filter selects are native `<select>` (top layer, no conflict). New deps radix tooltip+popover. 91 vitest · tsc/lint/`next build` clean |
| Brand relocation: icon rail + desktop top bar (BR2–BR5) | Complete · frontend-only | Rail brand → **icon-only** (`h-9 w-9` accent square, matches nav footprint; title/subtitle removed). New **`DesktopTopBar`** ("ConfMail" · "Conference Email System", `hidden md:flex`, `left-[var(--rail-width)] right-0`, `--topbar-height` **56px** token, **z-30**) — mutually exclusive with the mobile bar (`md:hidden` vs `hidden md:flex`). Corner geometry: rail unchanged (z-40, full-height); bar starts after it → no z-50 Radix collision; `<main>` offsets `pt-14`/`md:pt-[var(--topbar-height)]`. Fixed latent `queue/page.tsx` `h-screen` overflow → responsive `calc`. Filter toggle left-aligned (constant `px-2`). ⚠️ `--topbar-height`/`h-14`/`pt-14`/`3.5rem` = 56px in 4 places (guarded by `layout-tokens.test.ts`). 106 vitest / 12 files · tsc/lint/`next build` clean |
| "Open in Zendesk" button | Complete · **NOT committed** (Sahil to commit) | Self-suppressing pill in email-detail badges row → opens the ticket in Zendesk agent UI (new tab). **Backend-serialized URL** (Option B): `_email_to_dict` emits `zendesk_ticket_url` from existing `ZENDESK_SUBDOMAIN`+`zendesk_ticket_id` (`…/agent/tickets/{id}`, null-safe, never raises) — keeps subdomain single-source, no `NEXT_PUBLIC_*` needed; `config.py` unchanged. `ZendeskLinkButton` (new) renders itself-or-nothing from a raw null-able URL. Spacing via wrapper `ml-auto+pl-3` (padding inside the button's border wouldn't separate pills); flex-wrap → own right-aligned line, left as-is. +4 backend tests (376/7 non-ml), `tsc`/`lint` clean, both-theme + real-ticket manual pass |
| Set status, no reply (new/open/solved) + Ctrl+Alt+X | Complete · on `feature/mark-solved-no-reply` · gates green | Resolve a ticket's Zendesk status WITHOUT a reply. Status-only transport `ZendeskSender.set_status_only` (PUT status, NO comment, per-status `ai_status_*` tag, safe_update) + `POST /emails/{id}/set-status` (`SetStatusRequest{status,set_by}`) mirroring `/send` guards+reconcile minus the comment — **no send gate** (nothing reaches the requester); closed→409, non-zendesk→501, transport failure→`SEND_FAILED` (re-triable). `solved`=terminal (new `EmailStatus.SOLVED`, migration-free String(32)); `new`/`open` move the bucket but keep the workflow status. FE: `SetTicketStatusButton` (outline split — primary **Solved**, dropdown New/Open/Solved) in EmailDetail for **live Zendesk tickets only**, independent of draft/[CHAIR] placeholders (it sends nothing); **`Ctrl+Alt+X` = mark solved** (committing action → modifier combo like Ctrl+Alt+S, `e.code==="KeyX"`, respects typing guard; bare keys stay open-UI-only) + ShortcutsHintPopover row; queue optimistic-advance + bucket move; queue-level failure notice now worded per action (reply "failed to send" vs status change "failed"). Draft textarea got `aria-label`. Backend **449/7** non-ml · vitest **161** · tsc/lint clean · rl_router_state reverted. **MERGED to origin/main 2026-07-24 (`74bc62f..68fa9af`, FF).** Rebased onto Sahil's ticket-route refactor: the set-status wiring moved into `EmailWorkspace.tsx` (advance = **navigate-on-success** via `onOpenTicket`, matching his approve/send model — not the old optimistic advance); queue-page conflict resolved to his version; Ctrl+Alt+X kept AFTER his relocated typing guard (destructive → must not fire mid-edit) + his AltGraph refinement. Test rewritten to the `/tickets/[id]` harness. **Live-validated: backend restarted from merged main (both `/set-status` + `/by-ticket` routes verified), demo coherent on :8010/:3010.** |
| Queue/review UX tweaks + optimistic advance | Complete · **MERGED to origin/main 2026-07-24** | Four frontend-only fixes on `feature/queue-ux-tweaks` (`e995b50..`, FF). **(1)** Live-status flicker: SSE was per-`EmailWorkspace`-mount, so /queue↔/tickets navigation reopened it and showed "Reconnecting" (its init state) until onopen — made the connection an app-session **singleton** in `useEmailQueueStream` (module-level, opened once, never closed on unmount; new honest `"connecting"` state); it now stays "live" across navigation. **(2)** Shortcuts popover: label → "mark solved", dropped the helper line, keys `shrink-0`/label right-aligned. **(3)** Ticket activity list (`TicketAuditTrail`): collapsed by default + expandable, and **newest-first** (sort by timestamp desc). **(4)** **Optimistic advance restored** (Sahil's ticket-route refactor had switched to navigate-on-SUCCESS, so reply/mark blocked on Zendesk): send + set-status now advance to the next ticket the instant the local gate passes and write in the BACKGROUND (failure → queue-level notice); reroute/reassign advance on success; retry-from-banner stays advance-on-success (deliberate); redraft never advances (regenerates in place). **(5)** Filters (search/lane/status/chair/source/zendesk-status) now **persist via localStorage** (were plain state → reset on the navigation remount). **(6)** Redraft (Retry): textarea now re-adopts the server draft when `draft_text` changes (was seeded once at mount → showed stale draft), is **read-only while `redrafting`**, and the SSE stream also invalidates `["emailByTicket"]` so a completed redraft refreshes the open ticket immediately (not on the 15s poll). Commits `03aa039`/`5457f64`/`16e39f7`/`4a4c97a`. tsc/eslint clean · vitest **167** · rl_router_state reverted. Frontend-only → no backend restart needed (demo backend already at merged main). |
| `scripts/recovery/` category + internal-note discovery | Built · **NOT yet run on real Zendesk** · uncommitted (Sahil to review/run/commit) | NEW script category `backend/scripts/recovery/` (one-off incident-recovery/data-fix scripts, README with house rules, output gitignored) + `find_internal_notes.py`: finds comments an agent posted as `public: false` internal notes that were meant as public replies. **Read-only by construction** — `READ_ONLY` const + `assert`, and an httpx `event_hooks` guard raises `WriteAttemptError` on any non-GET before send. Reuses `credential_provider` for auth (no duplicated OAuth). **Two-phase whoami gate**: bare run = `GET /users/me.json` then STOP (nothing searched); proceeding needs `--confirm-author-id` **matching** the resolved id (chosen over a y/n prompt — forces an affirmative act on a value read off phase-1). Enumeration = **incremental cursor export** (Search API rejected: indexing lag + 1,000 cap); rate limits/Retry-After honored, deleted skipped, `--max-pages` warns INCOMPLETE. Filter = author ∧ `public is False` ∧ window (`--days` 4; strict `is False` so ambiguous privacy is never queued for resend). Outputs `.md` (human) + `.json` (`schema_version: 1`, for a future republish script — no re-scrape). **+44 tests** (mocked API; checkpoint-halt asserted on the recorded call log). Republish/write-fix deliberately NOT built — gated on review with Marc. Suite: 521 passed; the 20 failures are pre-existing dev-SQLite drift (missing mig `d5e6f7a8b9c0` → no `policy_documents.conflict_report`), identical without these tests |
| Policy Citations panel: compact rows + visible manual invoke | Complete · committed to `main` | Panel "suddenly huge" had **no CSS diff** — `0c6f946` (2a) began serializing `retrieved_chunks`, waking a card branch dormant since `bc69bb35`; every ticket had been on the ID-pill fallback. Separately `chunks.slice(0,3)` hid manual invoke (forced policy is the **4th** chunk at `MAX_RETRIEVED_CHUNKS=3`) → cap removed, bounded server-side, **don't re-add**. Final: one line per policy, `title · category` (category = muted inline text, nullable → segment omitted, never `· null`), no inline preview, row-click → `PolicyDetailModal`; keeps Add Policy + durable "Added by you" marker. 216 vitest · tsc/lint/build clean per commit · `a69c3d7`→`9b2c32c`→`0ebf2ab` |
| Policy exclusion (remove a citation, re-draft without it) | Complete · 9 pieces · committed to `main` | `excluded_policy_ids` on `RedraftRequest` (no migration; persisted in `retrieval_context`). **Pre-filter only** — excluded chunks never reach the prompt, so `drafter.py` is untouched and no LLM re-validation was needed (unlike multi-forced-add, which was scrapped for that risk). ⚠️ Matched on **lineage ROOT**, not `policy_key`: `edit_policy` mints `{root}__v{n+1}`, so exact-key exclusions go stale on the very KB edit that triggers the sweep. **Forced fails safe, exclusion fails open** — hence root matching is mandatory here. **Exclusion always beats forcing** (else the append silently undoes the removal). Minimum-1 guard in the **endpoint** (409) — the redraft is async so a later check has no response to reject with; a standing forced policy rescues an empty set (the swap workflow). Sweep re-applies exclusions in `_compute`'s exact order and carries them forward. FE: removal **staged then batched** (per-click would race and drop earlier removals), X is a sibling not nested, last card's control disabled with reason. Backend 551 passed / failure set identical to baseline at every step · frontend 229 vitest · mutation-verified · `761477d`…`2791576` |
| "Active but superseded" stuck policy (fix `1a3cc8a`) | Complete · root cause confirmed on production DB | Chair-reported: a green **active** policy rejected every edit (`Only an active, current policy can be edited.`). The guard needs `status=="active"` **AND** `superseded_by IS NULL` — two independent columns — but `reactivate()` only flipped `status`, so **edit A → retire A__v2 → reactivate A** left a permanently uneditable row (1 row in prod, isolated, fully reproducible). `reactivate()` now clears `superseded_by`; **`supersedes` deliberately kept** (live claim vs immutable history; `revert-edit` walks it, and it's the only backward link) — mirrors `revert_edit`, which already did exactly this. `retire()` unchanged (soft-delete implies no successor; fabricating one would be a lie). UI: `active && superseded_by` → **`superseded` badge** replacing the misleading green pill, **Edit disabled** + tooltip + inline note naming the successor + `aria-describedby`; Retire stays enabled. ⚠️ gated on `isActive` — an **inactive** row with `superseded_by` is the normal post-edit base state (regression-pinned). +11 tests, mutation-verified. Backend **560/7** (20 failures = known dev-SQLite `conflict_report` drift) · frontend **264/36** · tsc/lint clean. Prod row policy_174 already remediated separately (direct UPDATE cleared `superseded_by`; edit access confirmed) — this change touched no data |
| Queue pagination `total` transient-zero — **2 defects** | Complete · both mutation-verified · frontend-only | `useEmailQueue` returned `total: data?.total ?? 0` with no `placeholderData`, so `total` read **0** for one render on every query-key change; `pageCount = Math.max(1, ceil(total/100))` turned that into **"1 page"** and the stale-page clamp reset the page. `Math.max(1, …)` fabricating a floor of 1 from "unknown" is the core defect — the clamp couldn't tell *"shrank to 1 page"* from *"count not loaded yet"*. **Defect 1 (reported)**: clicking a page number bounced to page 1 on the FIRST click, worked on the second — discriminator is purely **cache occupancy for that page's key** (the in-flight fetch still populates the cache, so click 2 reads `total` synchronously); reproduced via the Next chevron too. **Defect 2 (separate, PRE-EXISTING, found during verification — not a side-effect of fixing #1)**: the persisted queue page was **discarded on every hard reload** — cold cache ⇒ nothing for `placeholderData` to keep ⇒ clamp fired before the count arrived; proven by offsets **`[400,0]`** landing on page 1. Fixes: (1) `placeholderData: keepPreviousData` (**v5 helper**; v4's boolean is removed — verified against installed 5.101.1) — at the source so `total` stays honest for all consumers; no conflict with `staleTime`/`retry`; only EmailWorkspace's key ever changes; `isFetching` not exposed (0 consumers). (2) clamp **kept but gated** on `hasCount = total > 0` — **not `!isLoading`**, which is false both mid-placeholder AND on error. Also closes the error window `placeholderData` can't (`pending` only; on `error` v5 gives `data: undefined`). +10 tests mocking **only the network layer**; disjoint mutation failures (drop placeholderData → 3; drop guard → 3, incl. the genuine-shrink test landing on page 1 instead of the correct page 2). ⚠️ existing 4 pagination test files mock the hook with a constant `total` (why this shipped) — left as-is by design, now carry **SCOPE LIMIT headers**; timing-dependent cases go in `queue-pagination-clamp.test.tsx`. Frontend **274/38** (from 264/36), tsc/lint clean |
| Drafter AoE time + top-k 5 (deployed to prod) | Merged to main (2026-07-29) · live on AAAI | Drafter injects authoritative **AoE (UTC−12)** "now" into system+user prompts, model told to treat it as the actual time, **no** usage/deadline instructions (`cb76364`, drafter-only; template/fallback untouched; baseline tests byte-identical when no time passed). `MAX_RETRIEVED_CHUNKS` 3→5 (`6c17204`, reverts `bda86eb`; grounding-breadth, E003 `hit@3==hit@5`). FF `d240962..6c17204`, branches deleted. **Backend-only** prod rebuild (no migration; frontend/db untouched; loopback preserved); prod `.env` pins top-k=5; in-container verified. **179 draft-side backlog redrafted** via paced (cap 3) full-pipeline `/redraft` — snapshot-backed (`redraft_backup_20260729_0940.tsv`) since `/redraft` overwrites with no `draft.history[]`: **179/179, 0 fallback** (2 timeouts retried). AoE confirmed live |
| KB policy previews: embedded newlines (1a–1d) | Complete · frontend-only · committed to `main` | Three KB surfaces collapsed policy bodies into one run-on paragraph. **Pure CSS gap — data verified clean DB→API→JS string**, no backend touched. Corpus separates list items with a **single `\n`, not a blank line** (reported policy: 8 newlines, 0 blank breaks; 89/157 policies contain `\n`), so markdown-style fixes wouldn't work and `pre-line` was rejected (flattens real nested indents). Fixed via inline `whiteSpace: "pre-wrap"` at `PolicyList` body (`499ee3c`/`a1756a7`), `AddPolicyPanel` similar-preview (`77b6ae3`/`6fc1699`), `PolicySelectPopover` **search-result** preview (`bebca9c`/`58ec552`) — **`className` byte-identical at all three, no clamp/truncation change**. Site 3 was **not** in the original suspicion list (found by grepping every `.content` render) and has a **static** clamp with no expand toggle, unlike the other two. `HighlightText` inheritance verified against **server-rendered markup** across 3 branches incl. a snippet spanning a newline (all children inline, none override `white-space`); Radix Dialog safety verified from **compiled CSS** (clamp emits `overflow:hidden`+`-webkit-box` ⇒ height-capped, containers unaffected). ⚠️ **`EmailDetail.tsx` is NOT a 4th site** — `CitationCard` has rendered no body text since the 2026-07-26 compact rework; do not resurface. +14 tests / 3 new files, API-layer-spy convention, `toHaveStyle` only, **all mutation-verified with disjoint failure sets** (existing `PolicySelectPopover.test.tsx` stayed 25/25 under mutation, proving element independence). ⚠️ **jsdom does no layout** — tests pin the declaration + clamp class, cannot prove visual breaks; `textContent` passes either way (SCOPE LIMIT headers in each file). Long-token wrapping flagged, not fixed (see Open Blockers) |
| Zendesk tag semantics (edited vs verbatim) | **Merged+pushed** to main (2026-07-29, `b69cb3f`) | `ai_auto_replied` iff public **AND** `draft.is_edited` false (verbatim AI reply reached requester); `ai_drafted` = edited public reply OR any internal note. One-liner in `/send` keying on `is_edited`. +4 tests. ⚠️ needs prod rebuild to go live |
| Chair Suggestions → actionable workflow | Merged LOCAL, unpushed (`e611a07`) | Prompt-only: drafter `NOTES FOR CHAIR` now a concise ordered workflow (one step/line → box lists items); **"none" rule kept** so the FAQ-lane gate (keys on `notes_for_chair`) is unchanged. Live-verified. No code/DTO/FE change |
| Chair Suggestions panel: one box + bullets (B1–D) | B1/B2/C1 committed (`12dfab4`/`7f0dd60`/`6e3ff57`); **C2 + D uncommitted** | Frontend-only. Steps rendered as N separate tinted boxes → **one** neutral container (`role="group"`, disclaimer stays outside) + plain rows + a muted `aria-hidden` **text-glyph** bullet (not a CSS dot — `rounded`/`bg-` would trip B2's only-box sweep). **C2 = ⚠️ PRECAUTIONARY** leading-marker strip (`1. `/`2) `/`- `/`– `/`— `/`* `/`• `) so a model marker can't render as `• 1. Do X` — **not a fix for an observed bug**: zero real note text exists locally and **two live probes** (`gpt-5.6-terra`) produced no evidence (second = single-step, unprefixed); shipped because the prompt never forbids numbering + `MODEL_PROVIDER` is swappable + the guard is inert without a marker. Regex narrowings all mutation-proven: `\s+` mandatory (saves `-5 degrees`/`1.5x`/`*emphasis*`), `\d{1,2}` not `\d+` (saves `2026. Deadline moved…`), empty-result guard (bare `1)`), live-probe string pinned as a no-op. **D** restores urgent distinction as a **2px `--danger` left RULE, not a box** (tint rejected on measured contrast — dark `#2b0f0f` on `#21263a` barely separable); advisory gets the same border `transparent` for alignment only, pixel-identical to pre-D; bullet tightened `-mr-1`. ⚠️ **jsdom can't expand a `var()` shorthand into longhands**, which had made B2's `borderColor===""` sweep vacuous against a full `var()` border — rewritten to match cssText, now mutation-proven. **40 chair-notes tests**, suite **340/44 files**, tsc+lint clean |
| Continual Experience Learning (CEL) | Merged LOCAL, unpushed (`ac83e4e..121094f`) · SDD | Chair `[CHAIR:]`-gap edit → non-blocking bg learn (gated LLM judge+condense) → candidate internal policy → chair review via existing add-policy flow **pre-filled** (conflicts surfaced). New `policy_suggestions` table (mig `ed4fa4d333f2`), `experience_learning.py`, `SuggestionRepository`, suggestions endpoints, KB Suggestions section+badge. Gated **no-op w/o model**; BM25 conflict candidates; accept reuses `POST /policies`; never raises into approve. **608/7 non-ml**, tsc/lint/kb-vitest clean. Follow-ups: intents-on-accept, list N+1, dispatch tests. **Follow-on (docs catch-up): `suggestion_audit_logs` + `SuggestionAuditRepository`, both review endpoints wired — mig `f8b2c4d6e0a1`** (no FK on `suggestion_id`, by SQLite convention; superseded as head by `57b59f3ef990`) |
| `received_at` = insert time, not arrival time | Fix on `fix/received-at-source-timestamp` (3 commits) · **backfill script built, NOT RUN on prod** | `received_at` had only `server_default=func.now()`, so it recorded when the poller INSERTED the row; the correct value sat unused in `zendesk_created_at` (populated since mig `d2e4f6a8b0c1`, serialized by no endpoint). Fixed at all **four** ingestion paths — `_compute` (covers Zendesk full-pipeline **and** `/ingest`) + the adapter's two direct-`create_email` branches; key added **conditionally** so reprocess paths never re-stamp an existing row. ⚠️ **SQLite `DATETIME` discards `tzinfo`** → an offset-bearing time persisted 5h wrong (PG unaffected); now normalized to UTC. Prod: **3,231 rows**, all `source='zendesk'` (zero toy rows), every `received_at` inside one **11-day window** with **~1,690 on Jul 21** (bulk sync) vs real tickets back to Jan 2; drift **bimodal** (~50% at 1–6 min, rest weeks–months, **max 972d**), 4 outliers ≥400d hand-verified. `scripts/recovery/backfill_received_at.py`: dry-run default, `--execute`+typed `yes`, rollback JSON **fsynced before the UPDATE**, verify **inside** the txn. ⚠️ Its **column guard caught a real bug** — `updated_at` is `onupdate=func.now()`, so SQLAlchemy silently appends `updated_at=CURRENT_TIMESTAMP` and the backfill would have destroyed the forensic signal Workstream A relied on; now pinned to itself. **+68 tests** (37 ingestion / 31 recovery), all mutation-verified; **687 non-ml passed / 7 skipped** (3 failures = pre-existing `alembic`-missing env drift). **Next: prod dry-run — expect exactly 3,231 rows + 4 outliers, else STOP.** Piece 3 (date/conference filtering) parked until this is decided |
| Queue date-range filter (`received_after`/`received_before`) | Shipped · backend + frontend · `/probe-cal` removed | Ida asked for date filtering; built on the `received_at` fix (trustworthy only because of it). Two nullable params on **both** `/emails/queue` and `/emails/queue/facets`, sharing `_queue_conditions` (page and `total` can't disagree) **and** the route-level `_received_range` (the two endpoints can't resolve a window differently). Dates are **context filters, not facet dimensions**. Contract: bare `YYYY-MM-DD` = **whole day** (after→00:00:00, before→23:59:59.999999), explicit timestamp used **verbatim**, naive = UTC, inverted range = **422**. "Date-only" is decided on **RAW TEXT** — hence params typed `str` not `datetime`, since FastAPI would coerce `2026-01-20` to midnight and destroy the distinction. ⚠️ **Deliberately no `zendesk_created_at` fallback** — it's NULL for `/ingest`+toy rows and NULL comparisons yield NULL, silently dropping them. FE: first date lib (`react-day-picker`+`date-fns`); `calendar.tsx` needed **two** rewrites — token hand-wrap **and** Tailwind **v4→v3** syntax (registry emits `size-(--x)`/`--spacing()`, which compile to *nothing* on 3.4). `DateRangeFilter` uses an **Apply/draft flow** (commit-on-click proved unusable for range-building); wired via `usePersistedState`/`filterKey`/`contextParams`. ⚠️ **`npx shadcn add` silently overwrote `button.tsx`** (reverted hand-wrapping + swapped to `@base-ui`) — always `git status` after. ⚠️ **Explicit child color beats an inherited parent rule regardless of specificity**: outside-month days rendered identical to in-month until targeted as `[&_button]:…`. ⚠️ Client timezone twin of the SQLite `tzinfo` bug — `toISOString()`/`new Date(str)` both shift the day; date-fns `format`/`parse` only. **~100 new tests**, mutation-verified; the `filterKey` test was impossible until the control rendered. Backend **714 non-ml / 7 skipped**, frontend **374 / 46 files**, tsc+lint+build clean. **Conference filtering (Piece 2) still UNSCOPED** — blocked on a live read of real tickets to see if Zendesk `tags`/`custom_fields`/`ticket_form_id` carry a conference signal |
| Reply-visibility toggle removed (fixed per status) | **Committed + pushed to origin/main** (`8b85426`..`a1534ca`) · **NOT deployed** — prod push still pending, and gated on warning Marc/Ida about the public-send behavior first | Marc/AAAI: the chair public/internal switch is gone; visibility is now a pure function of the submit status — **Solved → always public · Pending/Open → always internal** — removing the silent mis-set in both directions. Frontend-only; one `REPLY_PUBLIC_BY_STATUS` map read at BOTH call sites (button `onAction(status)` **and** Ctrl+Alt+S, which reads the persisted `approveStatus` — different variables, so they could otherwise drift); typed `Record<SplitActionStatus, boolean>` so a new status fails to compile rather than defaulting to internal. **Soft-removed**: `SendVisibilityToggle.tsx` untouched on disk, import/JSX/`replyPublic` commented in place with restore notes (⚠️ persisted `confmail.replyPublic` may still hold a stale `true`). ⚠️ **`ALLOW_AUTO_SEND=False` is NOT a backstop** — `send_gate` authorizes any chair-APPROVED draft regardless of the flag, so **"Submit as Solved" now posts a customer-visible reply in prod**; the public path has **never been live-verified** (only internal notes, ticket 21567) — verify on a safe ticket first. Side effect: unedited Solved replies now tag `ai_auto_replied` not `ai_drafted` (correct per the 2026-07-29 rework). Docstrings corrected in `emails.py` + `types/index.ts`: `public` never was gated on ALLOW_AUTO_SEND, `target_status` never was unused (`ApproveRequest.target_status`'s "not consumed" note IS still accurate, left alone). Tests 9→14 in `approve-send-chain.test.tsx` — closes the flagged gap that **Pending/Open payloads were never exercised**; `it.each` × 3 statuses × both call sites + edited-draft case + no-visibility-control guard; mutation-verified with disjoint failure sets. **379 passed / 46 files**, tsc+lint clean, backend untouched. Deploy needs `docker compose build frontend` (bakes at build time) |
| Paper ID + author extraction ("Submission Details") | Complete · 11 commits `a6ef245..0ce1307` · backend + frontend | Extracts the submission number, OpenReview forum id, and author mentions (name/email/affiliation, each optional) from the email text; shown as a **self-suppressing** "Submission Details" panel between the Intent row and the conversation (nothing to show ⇒ renders NOTHING, no empty state). **Piggybacks on the existing distiller call** — new `SUBMISSION_NUMBER:`/`OPENREVIEW_ID:`/`AUTHOR:` lines beside INTENT/CONFIDENCE/QUERY, **no second model call**; regex over subject+body+sender only when the distiller did not run. ⚠️ **The two paths are MUTUALLY EXCLUSIVE by design, not an oversight** — regex is a fallback, never a supplement (an empty LLM answer IS an answer); `method` stays a 3-value enum, so don't merge them without adding per-field provenance. ⚠️ **SCOPE: text-only — NO OpenReview API integration**, no lookup or verification; on the regex path "Authors" is just the SENDER, not a verified paper author. Natural extension point if OpenReview API access ever lands. ⚠️ **Two identifiers, two hazards**: the number needs a cue word and must dodge the `AAAI-26`/year collision; the forum id is exactly 10 alnum chars and must anchor to `/forum?id=`/`/pdf?id=` only (`/group?id=` is a confirmed real false positive). Subject scanned before body (~28% of real numbers live there). ⚠️ **NULL ≠ examined-but-empty** — a persisted semantic that must survive DB→API→frontend uncollapsed (null never coerced to `{}`). Persisted on BOTH `emails` and `email_processing_results` (mig **`57b59f3ef990`**, downgrade exercised on SQLite AND Postgres); wired at ONE choke point (`_compute`'s record), so every entry point incl. both follow-up paths got it free. Backend **819/7** non-ml · frontend **426/48 files** · tsc+lint clean · every piece mutation-verified |

## Open Blockers (active)
- **NCSA Delta GPU access pending** — the self-hosted (`MODEL_PROVIDER=local`) drafter is implemented + mock-tested but not run on real GPU hardware.
- **Synthetic email dataset** — the policy corpus is real (93 chunks) but `data/emails/toy_dataset.json` and `data/eval/ground_truth.json` remain hand-written synthetic; eval numbers are on synthetic traffic. Real-ticket eval (Phase 7) uses gitignored PII data under `data/eval_real/`.
- **Zendesk integration COMPLETE — remaining items are operational only** — Pieces 1–5 built, mock-tested, and **proven end-to-end with real data** (live internal-note write-back to ticket 21567 verified 2026-07-19); overlap guard hardened. Before production: (1) apply migration `f7a1b2c3d4e5` (overlap-lock columns) to the real production DB — **already applied to the demo Postgres 2026-07-19** (demo now at head `f7a1b2c3d4e5`; additive columns, 47+6 rows intact, no data loss); (2) decide whether to enable background polling (`ZENDESK_POLLING_ENABLED`, default `False`) and/or `ALLOW_AUTO_SEND` for public auto-replies (default `False`) — both remain off by design. Nothing else outstanding.
- **UNDECIDED (not a blocker) — long unbroken tokens don't wrap in policy previews.** Measured on the live corpus: **24 tokens exceed 60 chars, longest 108** (Google Forms + AAAI PDF URLs, e.g. `int_reciprocal-reviewer-changes`). Word-breaking is **orthogonal to `white-space`**, so the 2026-07-29 `pre-wrap` newline fix neither caused nor worsened this — it is pre-existing and unchanged. The three clamped previews (`PolicyList` body, `AddPolicyPanel` similar-preview, `PolicySelectPopover` search result) are `overflow: hidden`, so a long URL is **clipped, never escaping its container** — cosmetic, not a layout break. If addressed, it is a `wordBreak: "break-word"` addition **decided as one unit across all three sites** (the `PolicySelectPopover`/`PolicyDetailModal` **detail** views already have it, because they render full text in a scrollable box where clipping hides content a chair must read before approving). Deliberately kept out of the newline workstream to avoid mixing an unrelated layout change into it.
- **UNDECIDED (not a blocker) — `button.tsx`'s primary variant is 4.47:1, marginally under AA.** Its `default` variant ships `bg-[var(--accent)] text-white`, and white on `--accent` (#6366f1) measures **4.47:1 in BOTH themes** (the accent tokens are the same hex in dark and light) against the 4.5:1 floor for body text. Deliberately NOT fixed when found, because it is an app-wide call affecting every primary button, not something to change from inside one component. The one-line fix is `--accent-hover` (#4f46e5, **6.29:1**). `calendar.tsx` already uses `--accent-hover` for its own selected-day / range-endpoint fills — an intentional divergence from the button, documented in its header, so a selected date is not the same indigo as a primary button until this is decided.
- **⚠️ Security — rotate the OpenAI key** — `backend/.env` (gitignored) holds a live-looking OpenAI key (`sk-proj-…`) under `LOCAL_MODEL_API_KEY` — rotation recommended.

## Session Update Instructions
At the end of EVERY session: (1) append/compress the phase entry under Phase History; (2) update the Current Status table; (3) run `type CLAUDE.md` to confirm the save; (4) report "CLAUDE.md updated — [phase] logged". Not optional — skipping it breaks project memory.

## Codebase Navigation
Before exploring unfamiliar code or checking cross-module calls, use codebase-memory-mcp's 
search_graph / trace_path / get_architecture tools instead of grep/read-by-file. 
Especially verify module boundaries (classifier/retriever/router/drafter/persistence/UI) 
via trace_path before changes that could cross them.